{
  "name": "astro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:juece": "cross-env SITE_ID=juece astro build --outDir dist && pagefind --site dist",
    "build:erp": "cross-env SITE_ID=erp astro build --outDir dist-erp && pagefind --site dist-erp",
    "build:yunque": "cross-env SITE_ID=yunque astro build --outDir dist-yunque && pagefind --site dist-yunque",
    "preview": "astro preview"
  },
  "dependencies": {
    "@payloadcms/richtext-lexical": "^3.88.0",
    "astro": "^6.0.0",
    "sanitize-html": "^2.17.7"
  },
  "devDependencies": {
    "@types/sanitize-html": "^2.16.1",
    "cross-env": "^7.0.3",
    "pagefind": "^1.5.2"
  }
}