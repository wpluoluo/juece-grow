# 交付后待办（follow-ups）— astro-page-copy-cms

> Change: `astro-page-copy-cms` ｜ REQ-0003 ｜ 编写时间：2026-09-21
> 本文件是**索引与优先级**，不是第二份问题正文：每条的完整事实、为什么本轮没做、开工第一步都在
> `.aiws/issues/problem-issues.jsonl` 对应行的 `Title` / `Notes` 里（AGENTS.md §4 禁双写，这里不复述）。
> 双审查（`review/spec-review.md`、`review/quality-review.md`）的逐条处置见各自的
> 「主 agent 处置（triage）」表。

## 1. 必须先做的两项（P2，且都有时间窗口）

| ID | 一句话 | 为什么在窗口内 |
|---|---|---|
| PROB-041 | 四条迁移期守卫（parity / render-diff / hero-diff / coverage）判据是「与导入前基线逐字一致」，交付后**第一次合法改稿就会全红**，而没有任何一处写明退役程序 | 退役动作本身依赖「基线仍是真值」这个前提，一旦开始改稿就再也做不了干净；推荐 a+b 组合（归档 + 改名并标注窗口） |
| PROB-039 | SEO `<title>`/`description` 的**产物落地值**不在任何门禁内（抽取器剥 `<head>`），验收 6 目前只有源码级对面 | 修法要同时动 `evidence/snapshot-pre-import/render/*.json`（P1 冻结基线），与 PROB-041 是同一次「基线退役」动作，分开做会把基线弄成两个版本 |

## 2. 需要 owner 拍板的（不要由实现者默认）

- **PROB-044**：e2e 的期望值全部来自被测端点或 DOM，端点自身在 e2e 层没有 git 内锚点。
  审查者建议的「期望值改读快照」**不采纳**——它与 PROB-034（e2e 不得锚话术）直接互斥。
  待决的是分工取向：**是否接受「话术真值只在门禁层有锚、e2e 只测结构与顺序」**。
  若不接受，出路是给 e2e 造非话术不变量（如逐页「渲染文本 ⊆ 端点文本集合」），不是恢复字面量。
- **PROB-043**：删 `project` 时页面文案应当「禁止删除 + 可读报错」（推荐）还是「级联删文案」（危险）。
  取向定案后才谈得上改 schema（列改可空 / FK 改 `restrict` 都要走 AGENTS.md §7 的扩展迁移）。
- **PROB-023**（本 change 之前就 OPEN）：erp / 云雀分站构建出的法务页、404、站点地图、订阅源与文章
  JSON-LD 仍署觉策主站品牌与域名。它与本次「文案入 CMS」是同一根问题（站点身份硬编码），
  但范围在**其余页面模板**，不属 REQ-0003 的四页。开工前需要产品决定：这三类页面是否也一并搬进 CMS，
  还是先在模板层按 `SITE_ID` 派生品牌常量。

## 3. 机械收口（P3，可批量，但都必须带红侧自测）

| ID | 一句话 | 第一步 |
|---|---|---|
| PROB-035 | 站点/页面清单在五只门禁里各硬编码一份，合同表的 `SITE_IDS`/`PAGE_IDS` 没被引用 | 五处改 import 合同表，并断言「故意加一页时门禁必须变红」 |
| PROB-036 | `PANEL_KINDS` 三处声明、`SITE_PROJECT_SLUG` 跨 app 两处声明，漂移无门禁 | 先加一只键集相等断言并入 `astro-copy-agents9.mjs`，不改架构 |
| PROB-037 | `client-bundle` 的「CMS 地址已内联」谓词被任意 http 字面量满足，当前绿灯是巧合为真 | 谓词改成断言 `.env` 里 `PUBLIC_CMS_ORIGIN` 的确切串逐字出现 |
| PROB-038 | 163 行投影纯函数零单元测试，回归定位粒度只有「12/12 里有一处不等」 | 先定 runner（依赖变更须对照 AGENTS.md 红线），再补双向互逆 + 两条分支用例 |
| PROB-040 | e2e 写死端口，门禁读 `.env`，换端口时各测一站 | `origins.ts` 与门禁同源；注意它同时服务 leads 线（PROB-005/006） |
| PROB-045 | 验收 1 的「字段均有中英双语 label」只有 `en:` 计数（115/94/33/41），无逐字段断言 ⇒ 新增字段漏 label 时八道门禁全绿 | 在 `scripts/astro-copy-agents9.mjs` 加「每个 `name:` 字段对象体内同时有非空 `label.zh` 与 `label.en`」的断言（含 group/array/blocks 嵌套），并按 PROB-027 口径抹掉一个 label 咬红侧；**别退化成 `en:` 与 `name:` 计数相等**（PROB-037 同类的空谓词） |

## 4. 交付轮自身的记账残留（归档时的终局处置见 `verify-before-complete.md` 文末「收尾状态」第 3 条）

- **台账尾部日志的行待补（字节层已工具化收口）**：appender 不能登记自己的日志（写行时该日志还不存在）。
  P12 补登 `359`–`391`（33 条）、P13 补登 `392`–`416`（31 条）、**P14 补登 `417`–`452`（57 行，台账 349 → 406 行，21 只 appender，日志 `454`/`455`）**、**P15 补登 `447b` + `453`–`506` 里除 `457`/`472` 的 54 份（54 行，台账 406 → 460 行，22 只 appender，日志 `508`/`509`）**。
  **仍欠台账行的只剩本轮尾部**（P15 起即 `507` 之后那批）。**终局：本 change 在 P15 之后即归档关闭，不存在 p16 ⇒ 这批行确定不再补登**——随 `752d626` 入库的是它们的**字节**，未入账的只是台账**行**；且 change 目录搬运后 `448`/`481`/`append-verification-pN.mjs` 里写死的 `.aiws/changes/astro-page-copy-cms/…` 路径一并失效，日后若要续登须先把这三只工具改指 `…/archive/2026-09-22-astro-page-copy-cms/`。清单的唯一真值是 `448` 的 stdout 逐份点名，**不在此写死数值**——P14 那次读到的 `有字节但台账无行=31` 与终局的 `编号日志总数=462 归档有字节=462` 都已随本轮入账过期。逐条红/绿归类见 `verify-before-complete.md` §7 末。
  字节层不再依赖手工补抄：`448-audit-logs-missing-from-ledger.mjs`（第三只审计器，从盘上日志出发，把「字节是否已归档」当门禁、
  「台账是否有行」当信息项）实测出 55 份两头都不在账上 ⇒ `449-mirror-all-logs.mjs` 一次全量镜像收口
  （`tmp 侧=426 归档侧=426 复制前缺=55 新复制=55 复制后缺口=0`；选择正则修宽后复跑 `428/428 新复制=0 已存在且等值=428`）。
  此后每批续窗口统一用参数化的 `481-mirror-tail-window.mjs <LO> <HI>`（取代 `436`/`447`/`463`/`472` 四份写死窗口的副本；上界必须小于自身编号），
  再用同批的 `cp` 封口它自己的日志。终局那支刻意**不落 tmp 日志**的审计只需出示 `归档缺字节=0 判定=OK`（份数不写死）⇒ 4.3 提交后没有一份日志会随 tmp 消失。
  **补登时的七个已知坑**：① 若某行的 `artifact` 已在 `evidence/logs/` 里（本轮尾部就是这种），
  appender 的镜像步骤必须跳过自拷贝（`copyFileSync` 同路径在 Windows 上会失败），只断言在盘即可；
  ② 镜像工具自己的日志落在窗口外，需随下一轮 `cp` 处理，别把它算进那次运行的「已镜像」计数里；
  ③ 选择日志的正则要覆盖**带字母后缀**的编号（`236b`/`447b`/`463b`）——`^\d+-` 曾静默漏掉整类文件，
  而这正是这类审计器唯一存在的理由（`448`/`449` 已修为 `^\d+[a-z]*-`）；
  ④ 把红留在盘上时要连退出码一起留：`461` 那次抛错的运行只有堆栈没有 `_exit=1`，
  而 p14 起的自检要求行的 `exit_code` 必须字面出现在工件里 ⇒ 那一行要么按成功复跑记 `expected=0`，要么进排除表，**不许凭记忆补 1**；
  ⑤ 「归档缺字节=0」这一维**永远不可能由带日志的运行看到**——那一支运行自己的日志当时还没写完。
  要出示归零证据就让它只打到 stdout（退出码取 `${PIPESTATUS[0]}` 而非管道末命令），否则每一轮都会稳定报「缺字节=本轮自己那批日志的份数」而看不出是否真的漏了东西。
  ⑥ 预检取退出码时**别只认 `_exit=` 结尾的键名**：`482` 那次用的是 `mirror_all_reexit=0`（键名以 `exit=` 结尾但前面不是下划线），
  只匹配 `_exit=` 会把「确实落了退出码」的工件误判成没落，进而误判成不能入账（P15 首跑即红在这里）。判据不变、键名匹配放宽为 `[a-z0-9_]*exit=`。
  ⑦ 口径同步脚本的「全文件旧口径残留」扫描**不能放宽**，但要知道它会命中历史串：P15 的 `514` 命中 4.4 里抄旧记忆正文的「406 行 / 21 只 appender」，
  我新加的那句解释又抄了一遍 ⇒ `516` 同因再红。正确处置是把引用**改写字序**（「406 行 / appender 21 只」）或删掉引用，而不是把扫描范围缩到 3.10 那一行。
- **一处 note 计数错的更正（已改，留此备案）**：p13 第 31 行把「预检第 3 次运行覆盖 30 条」写成「31 个 artifact」，
  由 `426-fix-p13-note-count.mjs` 一次改齐 appender 与台账两处并复跑 no-op（`除 note 外字段不变=true 键序不变=true`）。
  它同时点明一个结构事实：**appender 也无法自查它最后写下的那一条 ROWS**——预检永远少一条。
- **`apps/e2e` 没有 typecheck 入口**（实现方在 `evidence/selfservice-and-negative-build-p9.md` §10 自陈，
  spec-review 亦登记）⇒ e2e 侧类型回归目前不在任何门禁内。补一条 `pnpm --filter e2e typecheck` 并并进
  `AI_WORKSPACE.md` 验证入口即可，属小票。
- **`.aiws/changes/astro-page-copy-cms/analysis/` 与 `patches/` 是空目录**（上一批清理 `chore(cleanup)` 删 6 个空目录之后的新残留）：**本轮 4.1 收尾已删**——`rmdir` 两目录 `rmdir_exit=0`，删除后目录清单只剩 `design.md` / `evidence` / `metrics.json` / `proposal.md` / `review` / `tasks.md`（日志 `407`，含删除前的 `ls -A` 空值证明）。空目录不被 git 跟踪，故此动作不影响任何 diff。

## 5. 明确**不**列为待办的事（避免后人重新发明）

- 线上迁移与内容导入**不是**遗留缺陷，是有前置清单的运维动作 ⇒ 见 `evidence/release-prerequisites.md`。
- 「台账覆盖率 100%」这个说法不成立也无需成立：正向 `appender ⊆ 台账` 已 `missing_total=0`，
  反向 10 条 uncovered 全是 P1 轮 `gen-verification-p1.mjs` 直写的日志 20–32（结构上扫不到），
  规划轮 01–10 从未入账（见 `verify-before-complete.md` 的起点声明）。
