> **勾选态唯一真值 = 本文件**（`aiws change status/validate` 读的也是它）。同目录原有一份 `tasks/tasks.jsonl` 派发台账（26 条 status 全 `pending`，只能用 `aiws change tasks execute/complete` 推进而无人推进），与本文件构成第二份账 ⇒ 已随交付轮删除，理由与逐条处置记在 `review/spec-review.md` 的「主 agent 处置（triage）」表与 PROB-042。

# Tasks: astro-page-copy-cms

> Title: Astro 公开站页面文案入 CMS（每页一个结构化集合 + 首页 hero/CTA 可排序）
>
> Created: 2026-09-20T09:57:36Z

## 0. Preflight

- [x] 0.1 阅读并遵守 `AI_PROJECT.md` / `AI_WORKSPACE.md` / `REQUIREMENTS.md` / `AGENTS.md`（含 §4 禁兜底双写、§5 命名三层映射、§7 schema 演进红线、§9 自检清单）
- [x] 0.2 运行门禁校验：`aiws validate .` → 实测：规划轮首跑 `exit=2`（四条：真值文件 `REQUIREMENTS.md` 漂移、`Contract_Row` 缺 `Problem_ID` 绑定、plan 的 `Problem_ID` 行夹带说明文字被判与 proposal 不一致、`Plan section is too long (24 steps > 8)`）；修正后复跑 `✓ aiws validate: F:\juece-grow` `exit=0`（`04-validate.log`）
- [x] 0.3 真值文件已变化（`REQUIREMENTS.md` 新增 REQ-0003 与两处口径修正），同步基线：`aiws change sync astro-page-copy-cms` → 实测：`✓ aiws change sync`，`Changed files: - REQUIREMENTS.md`，`sync_exit=0`（`03-change-sync.log`；stamp 落 `.aiws/tmp/change-sync/20260920-102508Z-astro-page-copy-cms.json`）
- [x] 0.4 在 `proposal.md` 填写主索引绑定：`Change_ID=astro-page-copy-cms` / `Req_ID=REQ-0003` / `Problem_ID=PROB-024` / `Contract_Row=Req_ID=REQ-0003,Problem_ID=PROB-024` / `Plan_File` / `Evidence_Path`（归因改为 PROB-024：它是本 change 实际修掉的缺陷；PROB-023 只登记不并入）
- [x] 0.5 计划文件 `.aiws/plan/2026-09-20_10-04-01-astro-page-copy-cms.md` 已落盘，绑定字段与 proposal 一致（校验器已复核通过）
- [x] 0.6 计划质检：`aiws plan-verify` → 实测：`ok: plan verification passed` `exit=0`（`01-plan-verify.log`）。注：`plan-verify` 与 `validate` 的判据不同——前者放行了带 24 条编号子项的旧版 Plan，后者按编号条目 ≤8 拒绝 ⇒ 章节粒度以 `aiws validate .` 为准
- [x] 0.7 严格校验：`aiws change validate astro-page-copy-cms --strict` → 实测 `ok: change validated` `exit=0`（`05`）；加跑 `--strict --check-scope` → `exit=0`，本规划轮全部改动（`REQUIREMENTS.md`、`docs/03-data-model.md`、两份台账、`.aiws/changes/` + `.aiws/plan/`）均落在 9 条机读 allow-list 内（`06`）；再加 `--check-evidence` → `exit=2`，报的 11 条全部是**交付前**才该存在的证据与双审查文件（规划轮不产出，非空签）（`07`）

## 1. 需求/问题合同

- [x] 1.1 `REQUIREMENTS.md`：REQ-0003 已写入 Backlog（背景/目标/非目标 + 8 条验收）。交付时移入「已完成」并勾验收框
- [x] 1.2 `.aiws/requirements/requirements-issues.jsonl`：REQ-0003 合同行已追加（`Spec_Status=READY`、`Impl_Status=TODO`）
- [x] 1.3 `.aiws/requirements/CHANGELOG.md`：追加 REQ-0003 登记行（2026-09-20）
- [x] 1.4 修正 `REQUIREMENTS.md` 文末治理项说明的 PROB 编号区间 —— 原写死 `PROB-001..011` 而台账实际已到 022（且本 change 还会到 024），改为「PROB 编号以该台账当前内容为准，逐轮追加，不在本文重复声明区间」，消除每轮必漂的固定上界
- [x] 1.5 登记 `.aiws/issues/problem-issues.jsonl`：追加 **PROB-023**（法务页/404/站点地图页/feed/文章 JSON-LD 品牌与域名硬编码，`Status=OPEN`，owner 裁决另立轻量 change）与 **PROB-024**（`features.astro:127` 拼出 `cap-tagyb` 而 CSS 为 `.cap-tag.yb`，实测三站各中一处：`features.ts:155`/`:316`/`:476` 各有一条 `tagTone: 'yb'`，`Status=OPEN`，由 2.16 修后转 DONE）。台账 `22 行 → 24 行`，追加脚本 `.aiws/tmp/astro-page-copy-cms/append-prob-023-024.mjs` 复跑第二次输出 `no-op` ⇒ 幂等已证，只追加未改写既有行
- [x] 1.6 `docs/03-data-model.md`：修「带 sortOrder」表述 —— §2 Block 行、§3 Page/Block 草稿行、§4 设计原则三处统一改为「展示序即 Payload 数组索引序，不存排序列」；并补 §3 的页面身份＝集合身份、站归属用 `project` 而非新增站枚举（与 design D1 对齐）

## 2. 实现

- [x] 2.1 导入前快照（回滚依据）：`evidence/snapshot-pre-import/` 落 12 份 `(site,page)` JSON dump（`homeContent`/`featuresContent`/`solutionsContent`/`pricingContent` × juece/erp/yunque）。实测：生成器 `.aiws/tmp/astro-page-copy-cms/dump-content-sources.mjs`，`node` 直跑 `exit=0`（`20-dump-content.log`），产出 12 份 `{page}.{site}.json` + `manifest.json`，叶子字符串合计 **1374**（与规划轮实测一致）、总字符 13922；纯 Node 靠 `module.registerHooks` 把 `content/*.ts` 的 `import { siteId } from '../site'` 换成常量桩绕开 Vite 专有 `import.meta.env`（桩值不参与选记录，dump 内容不受影响）。两项复核：复跑一次 `manifest.json` sha256 不变 ⇒ 输出确定；`features.ts:158` 内嵌 ASCII 双引号在 `features.juece.json:188` 正确转义为 `\"能跑\"` ⇒ 无文本切分损失
- [x] 2.1b 渲染层基线（`3.4` 的对照面）：同一目录 `evidence/snapshot-pre-import/render/{site}/{page}.txt` 落三站四页**切换前**可见文本基线（12 份）+ `render/_index.json`（逐文件 sha256/行数/字符数 + `gitRev` + 抽取器自身 sha256）；抽取器 `scripts/astro-copy-render-text.mjs` 为持久脚本，切换后原样重放比对。实测：前置 CMS 在 `:3000`（三站已发布文章 5/2/2，首页 blog 区块非空）；`pnpm astro:build`/`:erp`/`:yunque` 依次 `exit=0`（`24`/`25`/`26` 日志，pagefind 索引 13/10/10 页）；抽取 12 份共 **1269 行 / 20275 字符**（`27-extract.log`）。四项复核由主 session 独立重算（`32-verify-integrity.log`）：① 12 份逐个非零字节且 sha256/行数与索引一致 ② 幂等——含「删掉抽取器两处不可达兜底（`statsOf` 的空串三元、`index.files ??=`）」后重放，12 份 sha 仍逐字节相同 ③ dump↔渲染交叉核对命中（`juece/home.txt:13`、`juece/features.txt`、`yunque/pricing.txt:18` 三句且他站无）④ `diff juece/features erp/features` 152 行 ⇒ SITE_ID 生效。**动态内容裁决：不过滤**——首页 CMS 文章行与页脚 `© 2026` 原样入基线，因本 change 不动文章与 `getArticles` 调用，两侧应当逐字相同；若 3.4 在此处出差异按「读路径或 DB 被动过」查根因，不得加白名单（§4 禁兜底）。过程记录：首轮重放把 `outDir` 漏写 `/render`，在快照根造出 12 份游离 `.txt` + `_index.json`，`cmp` 证明与基线逐字节相同后删除（负向副作用如实登记为 `28-reextract.log` 的 `exit=1` 缺 HTML 硬失败用例）
- [x] 2.2 类型单一真值：新建 `apps/astro/src/types/pages/{home,features,solutions,pricing}.ts`（`wc -l` 实测 home 79 / features 38 / solutions 29 / pricing 38；首版记的 39/79/30/39 顺序错位且按 split('\n') 多算 1，spec-review W-3/W-4 已核），从 `content/*.ts` 原样抽出类型，数据文件只留 `import type`；`IconKey` 全仓唯一声明收进 `types/icons.ts:6`（此前 `home.ts:8` 声明、`solutions.ts:7` import、`pages/index.astro` 经 `home.ts` 的 `export type { IconKey }` 前向转发取用，转发垫片已删），`pages/index.astro:6` 改直连 `types/icons`。**本条原文的「`features.ts:10-14` 内联重复一份 IconKey」是规划轮的事实错误**，实测该行是 `FeaturePanel` 的五路 `kind` 判别联合，`features.ts` 从未声明 `IconKey`（已按 `git show 7279d55:apps/astro/src/content/features.ts` 复核并就地更正，design D3 同步改齐）。真实的重复是三处同形 `{ value, label }`（`features`/`solutions` 的 `hero.stats` + `home` 的 `cases.feature.metrics`），收进 `types/shared.ts:7` 的 `LabeledValue`。附带修好开工门槛：`apps/astro` 此前无可运行的类型检查入口（PROB-025），删 `tsconfig.json` 的 `baseUrl`、`paths` 改相对写法后，仓内 TypeScript 7 由 `exit=1`（`43`：TS5102 + TS5090）转 `exit=0`（`44`），并新增 `pnpm astro:typecheck` 入口（`49` `exit=0`）；PROB-025 已登记台账（`48`，24→25 行，复跑 `no-op` ⇒ 幂等）。零漂移证据：`pnpm astro:build` `exit=0`（`50`），juece 四页可见文本 sha 与切换前基线 **4/4 IDENTICAL、mismatches=0**（`68`）
- [x] 2.3 组件结构复制品收敛：`components/sections/PageHero.astro` 手写的 `stats?: { value; label }[]` 改 `LabeledValue[]`（`:6` import、`:14` 使用）；`PricingTable.astro:2` 的 `PricingPlan` 结构复制品改 `import type { PricingPlan } from '../../types/pages/pricing'`。实测另发现第三处复制品不在原计划清单里——`FeatureGrid.astro` 自抄了 `interface FeatureItem { title; desc? }`，与真值 `base.items`（`desc` 必填）已漂移成可选，故整个接口删除、改索引类型 `items: FeaturesContent['base']['items']`，漂移随单一真值消失。验证与 2.2 同批：`npx tsc --noEmit` `exit=0`（`44`）、`pnpm astro:typecheck` `exit=0`（`49`）、`pnpm astro:build` `exit=0`（`50`）、四页渲染 sha 零差异（`68`）
- [x] 2.4 CMS 集合 `page-home`：一比一镜像 `HomeContent`（现 `types/pages/home.ts:8-79`），双语 label、`project` relationship 必填、`status` select 默认 `draft`。**本条原文要求的 `seoTitle`/`seoDescription` 已按 design D8① 撤销**：实测四个 TS 类型都带页级 `meta.{title,description}`（一比一必然镜像进来，`PageHome.ts:29-40` 等），12 份快照 `meta` 全有值，且四页都把它直送布局（`index.astro:72-73` 等），`Layout.astro:44` 的 `seoTitle || title` 对这四页永不生效 ⇒ 那对字段是零消费者的第二套载体（§4 双写）。删后 `admin.useAsTitle` 改 `'id'`（Payload `validateUseAsTitle` 明确禁止嵌套路径，`meta.title` 不能当标题列）、`defaultColumns` 改 `['project','status','updatedAt']`。实测规模：`PageHome.ts` 443 行、`pageCopyShared.ts` 97 行（四集合共用 `ICON_OPTIONS`/`ACTION_OPTIONS`/`pageCopyAccess`/`uniqueProjectPerCopy`），leaf 字段 80 / group 15 / array 9 / hasMany 标量数组 3；覆盖度比对 `A\B=0`，`B\A` 仅剩 2 条「类型声明可选、12 份快照未填」的 `hero.primary.href`（三站主按钮均为 `action: 'lead'`）与 `products.spotlight.link.external`（`67`/`73`）
- [x] 2.5 CMS 集合 `page-features` / `page-solutions` / `page-pricing`：镜像各自类型（350 / 142 / 198 行，四集合加共享文件共 1230 行，均在 §4 限额内；较首版记录各 +1 行来自 2.7 补的 `hero.stats` `minRows:1`）。`FeaturePanel.kind` 用 Payload `blocks` 落地：`PageFeatures.ts:14-159` 的 `featurePanelBlocks` 五块 slug 即 `blocks`/`chips`/`bars`/`journey`/`agent` 五个 `kind` 值，`caps[].panel` 为该 blocks 字段（`minRows=maxRows=1`，判别键由 `blockType` 承载、**不另存 `kind`** ⇒ 无双写），blocks 字段实测 1 个 / block 5 个。D3 映射表逐条落地：`ICON_OPTIONS`/`ACTION_OPTIONS` 两组 `select`、`pegs[].pos`/`pegs[].icon`/`tagTone` 三个 `select`、`external`/`ghost`/`highlight`/`reverse`/`emphasis` 走 `checkbox`、`rows[].w` 与 `bars` 走 `number`、`blocks[].w`（CSS 几何值）走 `text` + `validateCssPercent`（`/^\d+(\.\d+)?%$/`）、`price` 走 `text` + `validatePrice`（纯数字或纯文字，挡 `3,999 元` 混合体）。跨站差异按 D3「可选留空、不按站裁剪」处理。覆盖度 `A\B=0` 且 features/solutions 的 `B\A=0`，pricing 仅 `plans[].currency` 一条（实测三站九档的键集合里都没有 `currency`）
- [x] 2.6 唯一性钩子：`pageCopyShared.ts` 的 `uniqueProjectPerCopy`（`CollectionBeforeChangeHook`）四集合共用并挂在各自 `hooks.beforeChange`；`project` 取 `data.project ?? originalDoc?.project`，对象形态取 `.id` 后 `Number()`，`update` 时以 `originalDoc.id` 排除自身，`req.payload.find({collection: collection.slug, where:{project:{equals}}, limit:2, depth:0, overrideAccess:true, req})` 命中他人即抛中文错误「该项目在本页面已有一条文案记录，请直接编辑现有记录」。页面身份 = 集合身份，未造 TS 里不存在的 `id`/`slug` 字段
- [x] 2.7 有序性与长度约束：展示序即 Payload array 索引序，全仓零 `sortOrder` 列（D2）。实测落到的行约束（`.aiws/tmp/astro-page-copy-cms/70-list-row-constraints.mjs` 逐字段列出）：`PageHome` — `hero.titleLines` `minRows:1`、`hero.stats` `minRows:1`、`hero.diagram.nodes` `maxRows:3`、`hero.diagram.chips` `minRows:3,maxRows:3`、`cases.minis` `maxRows:2`、`cta.rows` `minRows:1`；`PageFeatures` — `hero.titleLines` `minRows:1`、`caps[].panel` `minRows:1,maxRows:1`；`PageSolutions` / `PagePricing` — `hero.titleLines` `minRows:1`。开工时的偏差已修：`PageFeatures`/`PageSolutions` 的 `hero.stats` 只有 `required` 而漏 `minRows:1`，按 D2 补齐后 CMS 侧 `tsc` 与覆盖度脚本复跑仍 `exit=0`（`71`/`72`/`73`）
- [x] 2.8 富文本收敛（拆两半，本轮做前半）：schema 侧已完成——四集合统一 `hero.titleLines[]{text,emphasis}`，home 的 `em` 改名 `emphasis`，features/solutions/pricing 的 `titleA`+`titleEm` 折叠规则连同 `kind`→`blockType`、单对象 `panel`→恰好一行 blocks 一起固化成**唯一投影模块** `scripts/astro-copy-projection.mjs`（109 行，`toSchemaShape` 供导入与比对共用、`toReaderShape` 供端点解包；三条规则各自硬校验前提，前提不满足即抛，不静默跳过），把该投影接进覆盖度脚本后 `A\B` 由 8 条降到 **0**（`63`→`67`），投影规则不再是人肉断言。**后半（渲染器只留一种循环 + `types/pages/*` 的 `hero` 改 `titleLines`）随 2.14 一起落地**——它必须和「切读路径」同一次构建才有意义（改类型却不改数据源会立刻红），已按 D8③ 登记为 2.14 的前置动作。`*heading` 的字面 `\n` 按 D4 原样存 textarea，`index.astro:210`/`:243` 的 `<br>` 逻辑不动。**P4 后续变更**：该模块的消费方已包含 CMS 运行时代码（端点与导入脚本），故从仓库根 `scripts/astro-copy-projection.mjs` 迁入 `apps/cms/src/lib/pageCopyProjection.ts`（TS 化，162 行），并按 D8③ 新增读侧归一 `stripRecordKeys`/`fromRecord`；覆盖度脚本改指新路径复跑仍 `A\B total=0` `exit=0`（`91`）
- [x] 2.9 注册 + 类型再生：`payload.config.ts` 按仓内约定以 `{ ...PageHome, admin: { ...PageHome.admin, group: { zh:'内容与站点', en:'Content & Sites' } } }` 形式挂 4 集合（与 `Articles`/`Sites` 同组，非新分组名；分组仍走注册处叠加，集合文件内不写死）；`pnpm generate:types` `exit=0` 写入 `payload-types.ts`（相对 `HEAD` 净增 986 行、零删除，页面集合全为新接口）。生成器把 slug 单数化成 `PageHome`/`PageFeature`/`PageSolution`/`PagePricing` 等接口名，按 D8⑤ 接受（内部类型名不外泄到 JSON，为它改 slug 代价更大）。再生后 `npx tsc --noEmit` `exit=0`（`62`/`66`/`71`）
- [x] 2.10 建表迁移：`pnpm --filter cms exec payload migrate:create page_copy_collections` 生成 `apps/cms/src/migrations/20260920_121115_page_copy_collections.{ts,json}`（`index.ts` 自动登记），实测含 **39 张 `CREATE TABLE` + 15 个 `CREATE TYPE` 枚举 + 4 条 `project_id → projects` 外键 + `payload_locked_documents_rels` 加 4 列，零 `DROP`/`RENAME`**（`81`）。本地 `127.0.0.1:5434` 因账本 `batch=-1`（`85` 实测只有 `20260825_131753(batch=1)` 与 `dev(batch=-1)`）会被 Payload 自身拒跑，故按仓内既有处置把该文件 `up()` 的 SQL 提取（187 条语句 / 32542 字符）用 `pg` 驱动跑在**单事务**里（任一语句失败即 `ROLLBACK`），执行前断言库内无 `page_*` 表，执行后以 `information_schema` 复核：39 表 / 15 枚举 / 4 外键逐项齐全（`86` `exit=0`）。负向：复跑同一脚本 → `已存在 39 张 page_* 表，拒绝重复执行` `exit=1`（`87`），证明这条路径不可误伤已建好的库。账本未改动、未入新行。**全程只连本地容器库，未触碰线上**
- [x] 2.11 公开读端点 `apps/cms/src/app/api/v2/content/pages/route.ts`：`site` 必填（无 `?? 'juece'` 默认）、`page` ∈ 四值、project slug 三站全显式、只回已发布、未命中 404 `PAGE_COPY_NOT_FOUND`、异常 `logger.error` + 500；信封走 `lib/envelope.ts` 的 `ok/err` 唯一实现。响应前对记录施加 `apps/cms/src/lib/pageCopyProjection.ts` 的 `toReaderShape`（D8③：`blockType`→`kind`、恰好一行的 `panel` blocks 解包成单对象、剥 Payload 记账键、未填可选键折回不存在；解包不成立即抛，不静默），端点输出必须逐字等于 Astro 类型形态。**新增 `lib/pageCopyContract.ts`（39 行）作站点↔项目、页面↔集合的唯一合同表**（导入脚本与端点共用；顶部注明与 `articles/route.ts` 的 `juece: undefined` 聚合语义**故意不同**，主站不得吞掉分站文案），参数守卫 `asSiteId`/`asPageId` 只认表里字面值。实测契约（`102` `exit=0`，8/8 PASS，匿名无 cookie）：缺 `site`/未知 `site` → `400 INVALID_SITE`、缺 `page`/未知 `page` → `400 INVALID_PAGE`、`juece` 与 `erp` 的 pricing `meta.description` 各自独立、置 `draft` 后 → `404 PAGE_COPY_NOT_FOUND`（探针随即改回 `published` 并复核 200，不留测试数据）、原生 `/api/page-pricing` 匿名 → `403`（不可枚举）。CMS 侧 `npx tsc --noEmit` `exit=0`（`92`）
- [x] 2.12 导入脚本 `apps/cms/scripts/import-astro-copy.ts`（本条原文写的 `.mjs` 已按仓内 `payload run` 约定改成 TS；唯一入口 `pnpm --filter cms exec payload run scripts/import-astro-copy.ts`，零手填参数）：读 2.1 的 12 份 dump，先过 `toSchemaShape(page, dump)` 唯一投影再建 12 条已发布记录，按 `project + 集合` 幂等查重（快照文件缺失/不是对象即抛；`project` 由 `pageCopyContract` 的 slug 查库，查不到即抛；同 project 命中 >1 条即抛「唯一性钩子失效」）。实测：首跑 `created=12 updated=0`、读回 `page-home` 已发布 3 条，`exit=0`（`94`）；复跑 `created=0 updated=12` `exit=0`（`97`）⇒ 幂等已证，且第二次「改」也走同一条 `toSchemaShape`，不产生第二形态。脚本头注释显式写明**幂等语义 = 让记录等于快照 ⇒ 交付后重跑会覆盖后台手工改动，单条改稿请走后台**（`98`/`100` 那轮探针正是靠这条语义把改脏的记录还原的）。`features.ts:158` 内嵌 ASCII 双引号经 `JSON.parse` 直读对象，无文本切分
- [x] 2.13 parity 脚本 `scripts/astro-copy-parity.mjs`：逐 `(site,page)` 拉端点，与「快照 dump 过同一 `toSchemaShape` 后的期望形状」深比（D8④，比对两侧同形，投影只有一份实现），差异非零退出，输出 `12/12 逐字一致`。落地细节：CMS 地址取 `apps/astro/.env` 的 `PUBLIC_CMS_ORIGIN`（就是 2.14 之后构建实际要打的接缝，缺键/空值即抛，不给默认）；期望侧 = 快照 → `toSchemaShape` → `toReaderShape`，实际侧 = 端点 `data.copy` 原样取用（实测修正见 D8④：`toReaderShape` 二次施加会因 R3⁻¹ 只认数组 `panel` 而抛）；深比按路径报「值不等 / 数组长度不等 / 单侧缺键 / numeric 回成字符串」。**实测 `exit=0`，12/12 逐字一致，端点侧叶子字符串 1374 —— 与 2.1 快照的 1374 精确相等**（`96`）。咬合力证明（不是空跑）：`98` 把 `yunque/pricing` 的 `meta.description` 改脏 → `99` `exit=1` 且只报这 1 条差异、路径就是 `yunque/pricing: meta.description` → `100` 重跑导入脚本还原（`created=0 updated=12`）→ `101` `exit=0` 回到 `12/12`
- [x] 2.14 Astro 切读路径（含 2.8 后半）：先按 D8③ 把 `types/pages/{features,solutions,pricing}.ts` 的 `hero` 从 `titleA`/`titleEm` 改为 `titleLines[]{text,emphasis?}`、`home.ts` 的 `em` 改名 `emphasis`，四个渲染器只留一种 `titleLines.map()` 循环；再给 `lib/payload.ts` 加 `getPageCopy<T>(page)`（`!res.ok`/`!success`/缺 `data.page` 一律抛）+ `CMS_ORIGIN` 缺失即模块级抛（把 `:5` 注释的承诺变成代码）；四页 frontmatter 改 `await getPageCopy(...)`；删除 `content/*.ts` 四个数据文件。**实测落地**：`payload.ts` 127 行——P9 为 PROB-028 补 `connectFailure()` 后由 110 增至 127，与本条 3.9 的实测同口径（`getPageCopy` 三条失败路径各自独立抛、带 HTTP 状态与上游 `error.code`/`error.message`，零回退分支）；四页改 `const c = await getPageCopy<XxxContent>('xxx')`；`git rm` 掉 `content/{home,features,solutions,pricing}.ts` 共 **−1937 行**（524/587/276/550），`src/content/` 目录整体消失。「只留一种循环」按 D8③ 新增的落地口径执行：收敛的是两套标题**机制**，行内包裹元素按页不动（首页 `.line` span、内页 `{text}<br /><em>`），因为移动元素会改 Astro 作用域样式哈希归属 ⇒ 违反非目标第 3 条。逐字一致由**两条**门禁核（文本那条看不见 `span` 与 `<br/>` 的差别，故新增第二条）：`node scripts/astro-copy-render-diff.mjs` → `12/12 三站四页可见文本逐字一致` `exit=0`（`135`）；新增 `scripts/astro-copy-hero-dom.mjs` 抓 12 条 `<h1 class="hero-title">` 内部 HTML，切换前后 sha256 同为 `e582aa63e253effd`、`diff` 零差异 `exit=0`（基线 `119` + `evidence/snapshot-pre-import/hero-dom.json`，复测 `134`）。改动后回归：`pnpm astro:typecheck` `exit=0`（`138`）、三站构建各 `exit=0` 且日志 `ECONNREFUSED=0`（`131`/`132`/`133`）、parity `12/12` 叶子 1374（`139`）、覆盖度 `A\B total=0`（`140`）。（首轮复测记在 `128`/`129`/`130`，但那三轮退出码留在 harness stdout、没落进日志 ⇒ 换成带退出码的复跑编号，见 §7.2）。主 session 另收一处同源双写：`index.astro:359` 与 `articles/[slug].astro:59` 原直读 `import.meta.env.PUBLIC_CMS_ORIGIN`，改导入已校验的 `CMS_ORIGIN`。详见 `evidence/astro-read-path-switch.md`
- [x] 2.15 去 SITE_ID 兜底：删 `site.ts:168` 与 `astro.config.mjs:4` 的 `|| 'juece'`；`apps/astro/package.json` 的 `dev`/`build` 显式 `cross-env SITE_ID=juece`，删冗余 `build:juece`（实测 `.env` 无 `SITE_ID` 键，只删代码不改脚本会把根 `build.mjs:48` 与 e2e 前置打红）。**实测落地**：两处各加「缺键即抛」的显式分支（`typeof raw !== 'string' || raw.trim() === ''`），错误文案指向唯一正确入口（`cross-env SITE_ID=<juece|erp|yunque>` 与该包的哪些脚本已自带赋值），且与既有「未知 SITE_ID」检查分开报，不混成一条；`astro.config.mjs` 的未知值检查仍复用原 `SITE_DOMAINS` 判断不重复。`package.json`：`dev`/`build`/`preview` 三条显式赋值、`build:juece` 删除（全仓仅其自身定义与规划文档提到它，根 `astro:build` 走 `pnpm --filter astro build`，`scripts/build.mjs:48` 不受影响）；`cross-env` 是本包既有 devDependency（`build:erp`/`build:yunque` 已在用），**零新增依赖**。负向两条：`env -u SITE_ID … astro build` → `no_site_exit=1`，输出 `[astro] Unable to load your Astro config` + 上述中文可读文案（`125`）；`env -u SITE_ID … astro preview` → `exit=1`（`127`，`preview` 补赋值正是为消除这轮自引入的回归）。**本条删兜底另暴露一处同源的客户端运行时断裂**（客户端脚本传递引到 `site.ts`，此前一直由被删的兜底静默掩盖），当场抓到并按根因修毕、并新增产物级门禁，登记为 PROB-026，详见 2.17 与 `evidence/client-bundle-regression-fix.md`
- [x] 2.16 就地修 PROB-024：`features.astro:127` 的 `cap-tag${cap.tagTone ?? ''}` → `cap-tag${cap.tagTone ? ' ' + cap.tagTone : ''}`（现产出 `cap-tagyb`，CSS 是 `.cap-tag.yb`）。**实测落地**（改的是 `features.astro:128` 的 class 表达式，配色值仍来自 CMS 的 `tagTone`，未新增字段）：`136` 三站 features 产物各 **1 条** `class="cap-tag yb"` + **4 条** `class="cap-tag"`，缺陷串 `cap-tagyb` 计数三站均为 **0**；`137` 证第二半——源规则 `.cap-tag.yb` 随产物发布并被 Astro 编译成 `.cap-tag[data-astro-cid-fsswmxcn].yb`（三站同一份 `features.DcrV5jsl.css`），修前是单类名 `cap-tagyb` 故该选择器永不命中（tone 配色是死样式），修后是双类名故命中。取数坑记录在 `137`：作用域属性插在两个类名之间，按源码字面量 grep `.cap-tag.yb` 会得到「样式没发布」的假阴性。**这条是有意的视觉修复（死样式变活），不在逐字一致门禁范围内**——`135` 的文本门禁只比可见文本，class 不参与；`134` 的 DOM 门禁只锁 `<h1 class="hero-title">` 内部，两者都不因本条变红，实测也确未变红。**入账口径**：`136`/`137` 是人肉 grep、退出码没落进日志，故改写可机检脚本 `.aiws/tmp/astro-page-copy-cms/142-check-cap-tone.mjs`（四项断言任一不成立即非零退出）实跑三站全过 `cap_tone_exit=0`，台账只登 `142`。台账 `PROB-024` 本轮即转 DONE（不等 4.2）：`flip-prob-024-done.mjs` 只改那一行、问题台账仍 25 行（`149`），复跑 `no-op`（`150`）。收尾入账、REQ_SYNC 与偏差登记见 `evidence/astro-read-path-switch.md` §7
- [x] 2.17 e2e：新增页面文案读路径用例（分站读对内容、缺 `site` → 400、草稿不可见 → 404）；保留 `lead.spec.ts:11-16` 的 hero 文本断言。**实测落地**：新增 `apps/e2e/tests/page-copy.spec.ts`（166 行，5 个 `test(` 声明、参数化展开后 **8 条实跑用例**）——① `juece` 与 `erp` 的 pricing 互不串台（`meta.description` 与拼出的页头标题都不相等）；② `/pricing` 浏览器可见文本逐段等于端点 `copy.hero.titleLines[].text` 且 `<em>` 数与 `emphasis` 行数一致；③④⑤⑥ 缺/未知 `site`、缺/未知 `page` 四条 400 信封（匿名 `rawRequest` + `expectErr`，不带 cookie ⇒ 顺带证 fail-closed）；⑦ 原生 `/api/page-pricing` 匿名 403；⑧ 草稿不可见：先按 `slug=yunque` → project id → `where project equals` 精确锁定 yunque 那一行、断言其 `published`，置 `draft` → 端点 `404 PAGE_COPY_NOT_FOUND`，`finally` 复原 `published` 并复断 `200` + 标题相等（探针自恢复，不留测试数据）。`setup/global-setup.ts` 的预热表补两条（端点 `?site=juece&page=pricing` 与 `${WEB_ORIGIN}/pricing`），否则首条用例吃掉 Next/Astro 冷编译 ~48s 撞 30s 超时。取数坑：`pnpm --filter e2e test -- <path>` **不过滤**（pnpm 把 `--` 原样透传给 `playwright test`），实测该轮跑了全量 69 条，收窄要用 `pnpm --filter e2e exec playwright test <path>`。**这条的最大价值是抓到 2.15 引入的产品回归（PROB-026）**：`lead.spec.ts:11`/`:18` 两条浏览器用例红（`157`/`158` 各 `exit=1`），而 `162` typecheck、`164`/`165`/`166` 三站构建、`139` parity、`135` 文本、`134` hero DOM **全绿**——根因由 `158-lead-diagnose.mjs` 定位（`exit=0`）：`SITE_ID` 无 `PUBLIC_` 前缀 ⇒ Vite 不内联进客户端 bundle，`Layout.astro:297` 的客户端 `<script>` 为拿 CMS 地址 import `../lib/payload`，该模块 `import { siteId } from '../site'` ⇒ 站点解析进浏览器、`import.meta.env.SITE_ID` 恒 undefined，2.15 前被 `|| 'juece'` 静默兜住、删兜底后模块顶层抛错，整块脚本的监听器全丢，且已进生产产物。**修法走根因**（不新增 DOM 属性、不加兜底）：新建客户端安全模块 `apps/astro/src/lib/cmsOrigin.ts`（只读 `PUBLIC_CMS_ORIGIN`，缺键即模块级抛），`lib/payload.ts` 让出该校验、服务端自己改 import 它（唯一实现，无 re-export 垫片），三处消费方按 server/client 拆 import。复测：`164`/`165`/`166` 三站构建各 `exit=0`（`163` 是把脚本名打成不存在的 `astro:build:juece` 的空跑，非产品缺陷）、`167` hero-dom 整表 sha 仍 `e582aa63e253effd` 零差异、`168` render-diff `12/12`、**全量 e2e `170` `67 passed / 2 skipped`、`e2e_full_exit=0`**（lead 两条恢复绿）。新增持久产物级门禁 `scripts/astro-copy-client-bundle.mjs`（零参数，三站 `_astro/*.js` 不得含 `site.ts` 报错文案、Layout chunk 的 CMS 地址须为已内联字面量）：`173` `exit=0`；咬合力自测 `174`（注入桩串 → 三站逐一报 FAIL 且非零，还原 → 归零）`exit=0`。PROB-026 当场登记并结案（`175` 追加、`176` 复跑 `no-op`，问题台账 25 → 26 行）。详见 `evidence/client-bundle-regression-fix.md`

## 2A. 协同

- [x] 2A.1 规划期取证（1933 行字段清单、位置耦合、跨站差异、`payload.ts` 接缝）由只读 Explore 代理产出，结论已并入 `design.md` D2/D3 与 `proposal.md` 现状章节，不另存原始稿。**实测口径**：`evidence/collaboration-summary-20260920-224221Z.json` 记 `analysis: 0`（原始稿按本条约定不落盘，取证结论只以 design/proposal 的形式存在），`review: 2`
- [x] 2A.2 不使用 patch 草案（实现由 subagent 在工作树直接产出）。**实测口径**：同一份 collaboration-summary 记 `patches: 0` ⇒ 全 change 没有一份游离的 patch 草案工件
- [x] 2A.3 双审查落盘 `review/spec-review.md` + `review/quality-review.md`（Payload schema + 公开端点 = 高风险，须双审查）。**实测**：两份由独立只读子代理各自产出并落盘——spec 侧 `B-1`（BLOCKER）+ `H-1..H-4` + `W-1..W-9`，quality 侧 `HIGH-1..HIGH-3` + WARNING/INFO + `Gaps 1–9`；两份末尾各有「主 agent 处置（triage）」小节逐条判定（采纳并修 / 拒绝并给反证 / 转后续 PROB），拒绝项一律附独立复跑的反证。修复轮与红侧咬合证据分别在 `evidence/gates-promoted-and-log-archive-p11.md`、`evidence/home-hero-cta-order-e2e-p11.md`、`evidence/project-uniqueness-p11.md`；triage 的净结论（无剩余 HIGH blocker）与「仍未验证」清单见 `evidence/verify-before-complete.md` §3、§4

## 3. 验证（必须可复现，零手填参数）

> 前置：`pnpm db:up` && `apps/astro/.env` 含 `PUBLIC_CMS_ORIGIN`（实测 `http://127.0.0.1:3000`；`apps/cms/.env` 侧对应的是 `PUBLIC_CORS_ORIGINS`）&& CMS dev 在 `:3000`（`output: 'static'`，构建期必须能拉到 CMS）&& `pnpm astro:dev` 在 `:4321`。退出码由命令自身落盘，不过管道。

- [x] 3.1 `node scripts/astro-copy-parity.mjs` → 期望：`12/12 (site,page) 逐字一致`，`exit=0`。**实测 `exit=0`**（`96`），并附咬合力负向链 `99`（改脏 → `exit=1` 精确报该路径）/ `101`（还原 → `exit=0`），详见 2.13
- [x] 3.2 `pnpm --filter cms build`（红线，勿交 `aiws` 代跑——它把输出截到 2000 字符）→ 期望：`exit=0`，全量 TypeScript 检查无错。**实测 `cms_build_exit=0`**（`191`）：`✓ Compiled successfully in 26.1s` + `✓ Generating static pages (11/11)`，且生产路由清单里出现 `/api/v2/content/pages`（新端点确实进了构建产物，不是只在 dev 下存在）。**前置动作**：先停 `:3000` 上的 CMS dev——`next build` 与 dev 共用 `.next`，并发会互相覆盖。停的过程有个坑要记：harness 的 TaskStop 只杀了 `pnpm` 外壳，`next dev` 子进程（PID 61392）仍占着 3000，须 `taskkill /PID <pid> /F /T` 才放干净（放开的判据是 `curl` 连不上 ⇒ `curl_exit=7`，不是看任务状态）；构建完再重启 dev 并复核 `/api/v2/health` → `{"success":true,"data":{"status":"ok"}}`（启动日志 `192`，按 §7.2 的口径 dev 启动日志不入台账）
- [x] 3.3 三站构建 `pnpm astro:build && pnpm astro:build:erp && pnpm astro:build:yunque` → 期望：各自 `dist`/`dist-erp`/`dist-yunque` 产出、无 `ECONNREFUSED`；`node scripts/build.mjs` 亦 `exit=0`。**实测**：三站分别 `astro_build_juece_exit=0`（`193`）/ `astro_build_erp_exit=0`（`194`）/ `astro_build_yunque_exit=0`（`195`），逐日志 `grep -c ECONNREFUSED` 均为 **0**；`node scripts/build.mjs` → `build_mjs_exit=0`（`196`）且**没有走到 libuv 白名单分支**——`scripts/build.mjs:31` 那条「忽略退出阶段的 libuv 环境崩溃」的告警文案在日志里零命中，即 astro 那步是真 0 退出（这正是本条要求的判定：白名单命中不算绿）。`build.mjs` 内部同样跑了 `pnpm cms:build`，此时 dev 已在跑，两轮 `.next` 写入未致失败，跑完 `/api/v2/health` 仍 200。**构建产物被 `build.mjs` 重新生成 ⇒ 四道产物门禁当场复跑**，见 3.11/3.12 与 `197`–`201`
- [x] 3.4 渲染层逐字比对 `node scripts/astro-copy-render-diff.mjs`（切换前基线 vs 切换后，三站四页正文文本）→ 期望：差异清单为空。**实测 `exit=0`**：脚本零手填参数（自带 `rmSync` 重跑抽取器，三站分别从 `dist`/`dist-erp`/`dist-yunque` 抽到 `.aiws/tmp/astro-page-copy-cms/render-post/`），输出 `基线 12 份齐全；比对产物 → …render-post；差异 0 条` + `12/12 三站四页可见文本逐字一致（差异清单为空）`（`135`）。**本条门禁的覆盖面缺口与补偿**：文本抽取器只看可见文本，看不见 `<span class="line">` 与 `{text}<br /><em>` 的结构差别，而 REQ-0003 非目标第 3 条钉的是 DOM ⇒ 2.14 新增第二道门禁 `scripts/astro-copy-hero-dom.mjs`（持久脚本）：抓 12 条 `<h1 class="hero-title">` 内部 HTML、剥掉 `data-astro-cid-*` 哈希后做逐字节比对，切换前基线（`119`，落盘 `evidence/snapshot-pre-import/hero-dom.json`，sha256 前缀 `e582aa63e253effd`）与切换后（`134`）**sha 相同、diff 零差异 `hero_dom_diff_exit=0`**；该脚本对缺 HTML 与缺 `hero-title` 均非零退出，不会空跑变绿。两道门禁合起来才是 2.14 的逐字一致判定。**P8 更正（`200`）**：本条把 `astro-copy-hero-dom.mjs` 当成「第二道零参数门禁」记，但它入参是 `<distDir> <site> <outFile>`，裸跑只打印用法并 `exit=1`——`119`/`134`/`135`/`167` 那几轮是手拼三站参数 + `diff` 跑出来的，不符合本节标题「零手填参数」的验收口径 ⇒ 补持久包装器 `scripts/astro-copy-hero-diff.mjs`，见 3.12
- [x] 3.5 端到端自助改稿：后台改一条已发布文案 → 重建 → `pnpm --filter e2e test` 断言新串出现且旧串消失 → 期望全绿。**实测分两层，缺一不可**（`output: 'static'` ⇒ dev 服务器按请求重渲染、浏览器层当场能看到改稿，而交付产物只有重建后才变）：
  - **dev 层（进常驻 e2e）**：`apps/e2e/tests/page-copy.spec.ts` 第 6 组「后台自助改稿」（文件现 213 行）——`where slug=juece-grow` → project id → `where project equals` 精确锁定那一行（各要求恰 1 条，不按 `limit` 取第一条瞎改）→ 整份 `hero` PATCH（只换 `description`；Payload 对嵌套组是**替换**语义，故传回刚从 REST 读到的整个对象，不依赖深合并）→ 断言写入落库、公开端点读到新串 → `page.goto ${WEB_ORIGIN}/pricing` 断言 `.page-hero .hero-desc` 文本 **等于新串且不含旧串** → `finally` 用改前的 `hero` 原样 PATCH 回，并把整份 copy `JSON.stringify` 与改前快照**深比**（防「PATCH hero 连带损坏别的字段」）后再核一次页面文本回到旧串。实测：`234` `1 passed (53.9s)`、`selfserve_e2e_exit=0`。
  - **产物层（一次性回环）**：`.aiws/tmp/astro-page-copy-cms/235-rebuild-selfservice.mjs`——PATCH 标记串 → `pnpm astro:build` → `dist/pricing/index.html` 新串命中且旧串零残留 → `finally` 复原 PATCH → 再重建 → 旧串回来、端点同步复原。实测：`238` `RESULT 3.5-rebuild=OK`、`rebuild_selfservice_exit=0`（日志里可见「复原 PATCH 第 1 次连接失败（ECONNRESET）⇒ 重试」真的触发了一次）。
  - **首轮的红与恢复路径（如实登记）**：`235` 首跑 `rebuild_selfservice_exit=1`——约 90s 的构建期间 REST 连接空闲，Next dev 把它 `ECONNRESET` 掉了，复原 PATCH 未发出 ⇒ **库内留在测试串**（`3.5 重建自测 1789928555087 的`，长度 27）。恢复不靠手改：新增 `236-restore-pricing-hero.mjs`，从 git-tracked 快照 `evidence/snapshot-pre-import/pricing.juece.json` 把 `hero.description` 写回（`236b` `RESULT=OK id=1 的 hero.description 已回到快照值（长度 57）`），`237` parity 随即 `12/12 逐字一致` ⇒ 除那一个字段外无连带损坏。235 随即硬化两件事：连接层失败重试（`fetchJson` 三次、只重试 `cause.code` 属连接类的错）、开工先断言现值不是测试串（若是则拒绝跑并指向 236）。**`236` 首版自己也是红的**（`restore_exit=127`，抛在「hero 其他字段与快照不符 ⇒ 停手不写」）：它拿 REST 的 `hero` 与快照做结构比对，而 REST 会给数组行补 `id` ⇒ 假阴性；现版改为「按 `where` 精确定位 + 经公开端点验证」，并留 NOOP 分支以保证可重放。
  - **最终态复跑**：首轮四道门禁 `239` parity / `240` render-diff / `241` hero-diff / `242` client-bundle 各 `exit=0`，但**那一轮不算最终态**——`dist-erp`/`dist-yunque` 仍是 P8（`194`/`195`）的产物，而 PROB-028 改的 `lib/payload.ts` 是三站共用模块（只有 `dist` 在 3.5 回环里被重建）。补跑 `248` `astro_build_erp_exit=0` / `249` `astro_build_yunque_exit=0`（逐日志 `ECONNREFUSED` 计数 0）后重跑：**`250` render-diff `12/12`、`251` hero-diff `12/12`、`252` client-bundle 三站 `判定=OK`、`253` parity `12/12` 叶子 1374，四道 `exit=0` 才是本条的判定面**。全量 `pnpm --filter e2e test` → `243` `68 passed / 2 skipped`、`e2e_full_exit=0`（比 P6 的 67 多 1 条，正是本条新增的自助改稿用例）。
  - **边界（反转 P8 的一条 leftover）**：`scripts/astro-copy-hero-diff.mjs` 按逐字节比 hero `<h1>` 内部 DOM 与**导入前**基线 ⇒ 任何**合法**改稿都会让它变红。它是迁移期守卫（只在本 change 的切换期有意义），**不得**串进 `scripts/build.mjs` 的常驻构建链；本条的交付判定因此是「251 在三站全部重建后的最终态为绿」，不是「每次改稿都绿」。已在 `evidence/delivery-build-gates-p8.md` §8 就地更正。
- [x] 3.6 负向构建：`PUBLIC_CMS_ORIGIN` 临时指向死端口跑 `pnpm astro:build` → 期望非零退出 + 可读错误、不产出空白区块、不回退旧文案；跑完按**单行追加/改值**恢复 `.env`（禁止整文件重写）。**实测（首跑不达标，当场修到达标，见 PROB-028）**：`227` 手跑三条性质里两条当场成立——`negative_build_exit=1`、失败后 `apps/astro/dist` 内 HTML 计数 **0**（`html_count=0`，即不会发布空白区块），但报错全文只有 `TypeError: fetch failed` + `Caused by: connect ECONNREFUSED 127.0.0.1:59999`，既说不出是「文章列表」还是「页面文案」、说不出哪个 URL 与站点，也不表述「有没有回退」⇒「可读错误」这一半**不成立**。根因在 `apps/astro/src/lib/payload.ts`：两处 `fetch` 未处理连接层异常，undici 把真实原因挂在 `err.cause` 而 Astro 只打印 `err.message`。修法只改写错误文本（`connectFailure(url, label, err)`，带「哪份数据 + 完整 URL + 底层原因 + 构建期硬失败不回退旧文案 + 该去确认什么」，并以 `{ cause: err }` 保留原异常；不吞异常、不加兜底，其余三条失败路径原样不动）。修后同一条命令的报错（`231`）：`站点 juece 的文章列表 拉取失败：请求 http://127.0.0.1:59999/api/v2/content/articles?site=juece 未建立连接（connect ECONNREFUSED 127.0.0.1:59999）——构建期硬失败，不回退代码内旧文案。…`。同轮 `pnpm astro:typecheck` → `229` `exit=0`。
  - **`.env` 全程没动，也就不存在「恢复」**：Vite 的 `loadEnv` 下**进程环境变量优先于 `.env`**，故本条把 `PUBLIC_CMS_ORIGIN=<死端口>` 只注入**子进程的环境**、不落进任何文件（`.env` 未被本轮任何命令写过——脚本里只有 `spawnSync(..., { env })`，无对该文件的写调用），避开「为跑一次负向测试去改含令牌的配置文件」；门禁还**反向证明覆盖真的生效**（判定里要求死端口号 `59999` 出现在构建输出中），否则「`.env` 赢了」会伪装成绿灯。约束「`.env` 与 secrets 只追加不改写」因此天然满足。
  - **退出码判定只要求非零**：`231` 实测这条失败构建会以 `3221226505`（bash 侧看到 127）而不是 `1` 结束——退出阶段的 libuv 崩溃会改写码值（本机 Node 24/Windows 已知现象，与 `scripts/build.mjs:31` 的白名单同源）。按精确码判会造出一只随机闪红的门禁；正向构建仍按 3.3 的口径「白名单命中不算绿」。
  - 门禁入口（零参数、可复跑）见 3.13。
- [x] 3.7 端点契约负向：`curl` 缺 `site` → 400 `INVALID_SITE`；`page` 未知 → 400 `INVALID_PAGE`；仅草稿 → 404 `PAGE_COPY_NOT_FOUND`。**实测（`102` `exit=0`，8/8 全 PASS，匿名不带 cookie）**：缺 `site` 与未知 `site` 均 `400 INVALID_SITE`（未知值不回退主站）、缺 `page` 与未知 `page` 均 `400 INVALID_PAGE`、把 `yunque/pricing` 置 `draft` 后 `404 PAGE_COPY_NOT_FOUND`（随即改回 `published` 并复核 `200`，探针自恢复、不留测试数据）。另附两条本条未列但同源的契约：`juece` 与 `erp` 同页文案互不串台、原生 `/api/page-pricing` 匿名 `403`（不可枚举）。浏览器层的读路径断言仍归 2.17 / 3.5
- [x] 3.8 收口门禁链：`aiws change sync astro-page-copy-cms` → `aiws validate . --stamp` → `aiws change validate astro-page-copy-cms --strict` → `aiws change validate ... --strict --check-evidence --check-scope`（后两条默认不启，须单独跑并逐条核对越界清单，见 PROB-011）。**实测（四步链在最终态全绿，退出码由命令自身落盘、不过管道）**：① `388` `sync_exit=0` ② `389` `validate_stamp_exit=0` ③ `390` `ok: change validated` `exit=0` ④ `391` `ok: change validated` `change_validate_evidence_scope_exit=0`。**首跑曾红（`369` `exit=2`，记为 `failure` 不粉饰）**，报的 4 条 error 逐条消除：3 条指向不存在的 `evidence/verify-before-complete.md`（含合同行 `Evidence` 与 `Evidence_Path` 的悬空引用）⇒ 4.1 落盘该文件后消失；1 条 `.gitignore` 越界 ⇒ 在 plan 的 In Scope 显式补该文件（该负向豁免 `!/.aiws/changes/**/evidence/logs/*.log` 是台账跨 clone 可读的前提，见 spec-review H-1）后归零。**PROB-011 的越界清单逐条核对结果 = 0 条越界**：本轮所有改动落在 plan Scope 的机读清单内（`apps/{cms,astro,e2e}`、`scripts/astro-copy-*`、`REQUIREMENTS.md`、`docs/03-data-model.md`、两份台账、`.aiws/changes|plan/**`、`AI_WORKSPACE.md`、`.gitignore`）。链上复算的产物级门禁见 3.10 的 `393`（归档面 318/318 sha256 一致）
- [x] 3.9 AGENTS.md §9 自检：camelCase 三层映射、无兜底/双写、单文件 ≤1000 行（AGENTS.md §4 原文限额；本条首版误写成 1500，见 PROB-032）、SEO 三件套逐页齐全、线索仍在自有 Postgres、影响范围已说明。**做法**：不做人肉过一遍（§9 的措辞本 change 已被 PROB-027 打过一次「断言没实测」），改写成可机检脚本 `.aiws/tmp/astro-page-copy-cms/268-agents9-selfcheck.mjs`（零参数、12 条断言、任一不成立即非零退出）→ **实测 `268` `selfcheck_exit=0`，「自检 3.9：全部断言成立」**。逐条实测值：① **单文件限额**（**行数口径全 change 统一：`wc -l`；末行无换行符时 `wc -l` 少算 1，故报「`wc -l` +（末行无换行则 +1）」**——spec-review W-3 指出此前 `split('\n')` 与 `wc -l` 两口径混用且未注明，本条与 2.2 的行数都按此口径复量）——自研最大 `apps/astro/src/pages/features.astro=595` 行，四只 Payload 集合 443/350/142/198，投影 162，`payload.ts` 127，全部 ≤1000；**生成物单列并证来源**：`payload-types.ts=2022` 行（文件头含 `This file was automatically generated`）、`20260920_121115_page_copy_collections.ts=689` 行（签名 `up(...): MigrateUpArgs` 由适配器生成）+ 同名 `.json=8038` 行（drizzle-kit 形态，顶层键含 `dialect=postgresql` 与 `prevId`）⇒ 三条都是工具产物，不占 §4 自研限额，也不靠「我觉得它算生成物」豁免 ② **无兜底**——`git grep "|| 'juece'" apps/astro` 命中 **0**，`site.ts` 与 `astro.config.mjs` 各含显式 `trim() === ''` 抛错分支 ③ **无双写/无回退**——`apps/astro/src/content/` 目录不存在（旧数据源物理消失）+ 全仓 `from '../content/` 残留 import **0** + `payload.ts` 的 **3 个 catch 块逐个取体**（花括号配对）后首句分别是 `throw connectFailure(...)` / ``return `上游响应体不是 JSON：…` `` / `throw connectFailure(...)` ⇒ 只抛或只产诊断串，没有一条返回旧数据（这条取代了首版「全局正则计数相等」的假代理，首版实测红：同一模式在非 catch 上下文还有 2 处命中，计数永不相等） ④ **命名三层**——四集合 + `pageCopyShared.ts` 的全部 `name: '…'` 零 snake_case 命中，Astro 侧 `copy.<snake>` 取值 0 ⑤ **API 规范**——路径 `/api/v2/content/pages`、端点 `import { err, ok } from '…/lib/envelope'` 且全文件零手写 `Response.json`/`NextResponse.json` ⑥ **SEO**——`Layout.astro` 同时输出 `<title>`、`meta name=description`、`link rel=canonical`，四页 `title={c.meta.title}` + `description={c.meta.description}` **4/4** 取自 CMS 记录 ⑦ **线索主数据**——`scripts/astro-copy-agents9.mjs` §7 的谓词在 2A.3 triage 后收紧为**只看生产代码**：`touched`（`git diff --name-only HEAD` + `ls-files --others`，已限定 `^(apps|scripts)/`）里 `lead` 命中再剔除 `\.(spec|test)\.[a-z]+$` ⇒ **生产侧 0 个**，被剔除的测试命中如实打印而不静默放行（实测输出 `生产代码 0 个；只改过测试断言：apps/e2e/tests/lead.spec.ts`——该文件是 W-8 修复动的一行文案断言，leads 表/接口/schema 全未触碰）。首版判据（「diff 里 lead 相关 0 个」）在日志镜像进门禁后自己变红过：把测试断言算成「触碰线索主数据」是假阳性，故按生产/测试分层。红侧已咬测：`node .aiws/tmp/astro-page-copy-cms/354-agents9-lead-scope-negative.mjs` 造一个未跟踪的 `apps/cms/src/collections/LeadProbeScratch.ts` → §7 `exit=1` 且点名该文件，删除后复跑 `exit=0` ⑧ **影响范围**——`proposal.md` 的 `## 影响范围（Scope）` / `In Scope` / `Out of Scope` / `外部影响` 四小节齐全。**机检覆盖不到的两点（如实说明，不假装）**：`canonical` 的具体取值仍由 `Layout.astro:43` 的既有 `url || Astro.url.href` 决定，四页不传 `url` ⇒ 走 `Astro.url.href` 分支；这行本轮未改（`git diff` 证 `Layout.astro` 唯一改动是 `:297` 的 import 换模块），属既有行为而非本 change 引入，脚本只断言「三件套存在于输出中」而不重证既有语义
- [x] 3.10 机器可复核证据：`evidence/verification.jsonl`（逐条命令 + 期望退出码 + `.aiws/tmp/astro-page-copy-cms/` 工件指针），使 `aiws verify-bc` 不报 `legacy evidence assumed`。**实测状态（P15 收口，P9 时为 190 行）**：台账 **460 行**（每行 artifact 均已镜像进 `evidence/logs/`，由 `scripts/astro-copy-evidence-archive.mjs` 核对——最新复算 `台账行数=460 无 artifact 行=0 不同 artifact=451 … sha256 与原件一致=460 问题=0`（`512`），P14 收口时的复算为 406 行 / 397 不同 artifact（`458`），p13 收口时为 349 行（`421`），p13 入账前为 318（`393`），见 PROB-033），每行 `{command, exit_code, status, expected_exit_code, started_at, finished_at, timestamp_note, artifact, note}`，`status` 由 `exit_code === expected_exit_code` 算出 ⇒ 故意红侧（如 `227` 的负向构建）记 `expected=1` 即 `success`，真缺陷/自引入回归（`159`/`228`/`235`/`236`、打错脚本名的 `163`，`351` 的 §7 判据假阳性、`356` 我自己 note 复现 canary、`369` 的 P-V7 四条 error，以及 P13 入账的 `394`/`395`（appender 的 `ROWS` 引用模块级常量，把两只审计器求值搞崩）、`397` 两次（修脚本自己的断言计数与 eval 作用域）、`400`（B4 抓到 3.10 四处过期口径）、`408`（4.2 推进脚本的 MARKER 与实际写入文本不一致，自检在写盘后才炸）、`416`（本支的预检脚本自己把 `ROWS` 段切错一位），以及 P14 入账的 `422`（3.10 口径过期，B4 抓到 4 条）、`423` 首跑（配对正则里未转义的 `*` 使脚本自己 SyntaxError，红在未改盘）、`448` 首跑（实测 55 份日志既无行又无归档字节）、`451`（如实报出 449/450/451 自己尚未归档），以及 P15 入账的 `464`（`448` 的选择正则 `^\d+-` 静默漏掉带字母后缀的编号日志（`463b` 一类），它抓到的是审计器自己的盲点；修宽后的复跑 `465` 仍报唯一一处缺字节＝它自己那份还没写完的日志））记 `expected=0` 即 `failure`，不靠 note 措辞粉饰。**appender↔ledger 两向覆盖都已实测（注意：不含「tasks 每条 → 台账」那个方向，别把这两向读成 tasks 的每一条都被台账核过）**：正向 `node .aiws/tmp/astro-page-copy-cms/114-audit-ledger-coverage.mjs` → `AUDIT ledger_rows=460 missing_total=0`（22 只 appender 各自 `ROWS` 声明的行逐条 `command+artifact` 键比对，命中即在册；`510`，P14 收口时为 406 行 / 21 只＝`456`）；反向 `node .aiws/tmp/astro-page-copy-cms/221-uncovered-rows.mjs` → `ledger=460 covered=450 uncovered=10`（`511`），10 条 uncovered 全部是 P1 轮由 `gen-verification-p1.mjs` 直写的日志 20–32，反向审计器只扫 `append-verification-*.mjs` 故结构上认不到它们——不是丢账，也不打算为此再造一只 appender。**幂等已证**：每只 appender 复跑输出 `no-op`（P11-c `346`、P11-d `360`、P12 `396`、P13 `418`、P14 `455`、P15 `509`）。**已知口径限制（如实登记）**：appender 无法登记自己的日志（写行时该日志还不存在），故每轮尾部的若干份日志必然留到下一轮补账——P11-c 补登了 24 份（`320`–`344`）、P11-d 补登 14 份（`345`–`358`）、P12 补登 33 份（`359`–`391`），P13 补登 31 份（`392`–`416`）、P14 补登 57 行（`417`–`452` 那 54 份日志，含 423/426 的多跑拆行）、**P15 补登 54 行覆盖 54 份日志**（`447b` + `453`–`506` 里除 `457`（落盘时没有退出码行 ⇒ 进排除表，改由同一命令带退出码的复跑 `506` 入账）与 `472`（那支脚本没有自己的日志，正文在 `473` 里）之外的全部）。**P14 把这条限制拆成两件事分开收口，不再混为一谈**：`448-audit-logs-missing-from-ledger.mjs`（新增的第三支审计器，从磁盘上的日志反向点名，不看 ROWS 也不看台账）首跑实测 55 份编号日志既无台账行又无归档字节——P2P3/P3d/P5b/P6/P9b 五轮 appender 自己的日志就是这样连漏 5 轮，正向与反向两支审计器结构上都看不见「日志有、行没有」这一类。① **可逆性**（真丢的那一面）由 `449-mirror-all-logs.mjs` 全量镜像一次收口：`镜像窗口 <449：tmp 侧=426 归档侧=426 复制前缺=55 新复制=55 已存在且等值=371 逐文件 sha 与原件一致=true 复制后缺口=0`（`449`）⇒ 此后不再有任何一份日志会随提交丢失；② **入账**由 `append-verification-p14.mjs` 补登：`appended=57 declared=57 total=406 新镜像=4 已在册等值=53 failure=4`（`454`），入账前逐字预检见 `453`（它比 p13 那版多一条判据：每行 `exit_code` 必须真的出现在该工件自己的 `*_exit=` 行里，防止台账被写成第二真值）。留在下一轮的只剩本轮尾部的**行**：`448` 每次运行会把「有字节但台账无行」的清单逐份点到 stdout，本轮补登后剩下的就是本轮自己新写的这些（本轮终局数记在 `verify-before-complete.md` §7 行 37，不在本条复述——写死即随下一轮过期）。补登与续镜像一律走同一套。字节面由 `449-mirror-all-logs.mjs`（正则修宽后复跑：`镜像窗口 <449：tmp 侧=428 归档侧=428 复制前缺=0 新复制=0 已存在且等值=428`）与参数化尾部镜像 `481-mirror-tail-window.mjs <LO> <HI>`（本轮窗口自 `507` 起、上界取末支带日志的门禁，且必须小于自身编号——它看不到自己的字节；它取代 `436`/`447`/`463`/`472` 四份写死窗口的副本）加各支里的 `cp` 封口进库。选择正则 `^\d+-` 静默漏掉带字母后缀的编号日志（`236b`/`447b`/`463b`）已在 P14 修成 `^\d+[a-z]*-`（实测由 `464` 红到 `465`）；末次**刻意不落 tmp 日志**的审计判据是 `归档缺字节=0 判定=OK`——此前每支带自己日志的运行必报 `归档缺字节=1`（缺的就是它自己那份还没写完的日志），那是结构不是缺陷。另有 36 份日志按 `448` 的**声明式排除表**逐份写明理由不入账：规划轮 10 份（引 §0「台账起点 = 20」）、常驻 dev 服务日志 4 份（进程不退出⇒无退出码）、落盘时未记退出码 17 份（本轮新增 `457`）、命令字面量不可复现 5 份（`160`/`161`/`169`/`171`/`172`，其中 `171`/`172` 的红即 `client-bundle-regression-fix.md` §8.1 那句「apps/e2e 至今没有类型门禁」）。排除表自带一条反向自检：任何一份若已被入账就报「排除表过期」并红。⇒ 台账覆盖的是「可复现的验收判定」，不是「执行过的每一条命令」；任何「覆盖率 100%」的说法都不成立
- [x] 3.11 客户端 bundle 污染门禁 `node scripts/astro-copy-client-bundle.mjs`（P6 新增，零参数）→ 期望：三站 `_astro/*.js` 均「禁止串命中=0 / CMS地址已内联=true / 判定=OK」且 `exit=0`。**为什么必须有这道**：P-V1..P-V5 五道门禁全部只看服务端产物（端点 JSON、HTML 文本、hero DOM、构建退出码），而 PROB-026 那条断裂只发生在浏览器运行时——`SITE_ID` 无 `PUBLIC_` 前缀不会被 Vite 内联，客户端脚本传递引到 `src/site.ts` 就在页面运行时抛错，五道门禁同时全绿。**实测**：修后 `173` `client_bundle_gate_exit=0`（三站各 1 个客户端 chunk、命中 0）；咬合力自测 `174`（往三站 chunk 注入桩串 → 逐站报 `判定=FAIL` 且退出码 1；`finally` 还原 → 退出码 0）`client_bundle_negative_exit=0`；同轮全量 e2e `170` `e2e_full_exit=0`（67 passed / 2 skipped）。首轮一次性探针（日志 `169`）跑出同样结论后已固化为持久脚本，tmp 脚本删除 ⇒ 实现只有一份
- [x] 3.12 hero DOM 逐字节门禁的零参数入口 `node scripts/astro-copy-hero-diff.mjs`（P8 新增，零参数）→ 期望：`12/12 三站四页 hero <h1> 内部 DOM 与切换前基线逐字节一致` 且 `exit=0`。**为什么补这道**：见 3.4 的 P8 更正——抽取器要三个入参，`200` 裸跑实测 `hero_dom_exit=1`（只打印用法），P5/P6 的复测全靠手拼参数，交付时任何人按 REQ-0003 `Tests` 那行复制命令都复现不出绿灯。**做法（不重复实现）**：本脚本只做编排与比对——`rmSync` 掉上一轮 outFile 后按三站各调一次抽取器（HTML 解析、`data-astro-cid-*` 归一仍只有 `astro-copy-hero-dom.mjs` 一份实现），再把产出的 12 条与 `evidence/snapshot-pre-import/hero-dom.json` 逐键比字符串，任一不等即非零退出并打印「切换前 / 本轮」两行。**关键陷阱**：抽取器对已存在的 outFile 是**合并**语义，不先删就会拿上一轮残留凑满 12 条 ⇒ 假绿，故 `rmSync(postFile, { force: true })` 在串联之前。**实测**：`201` `hero_diff_exit=0`（12/12 一致，基线 12 条）；咬合力 `202`（往 `apps/astro/dist/index.html` 的 hero `<h1>` 注入一个多余 `<span class="probe-injected">` → `[hero-diff] 差异 1 条：✗ juece/home` 且退出码 1；`finally` 里逐字节还原并断言内容等 → 复跑回 0）`hero_diff_negative_exit=0`。本轮 3.3 之后跑，等于同时把「`build.mjs` 重新生成的 dist 也没动过 DOM」这件事钉住

- [x] 3.13 CMS 不可达负向构建门禁 `node scripts/astro-copy-cms-unreachable.mjs`（P9 新增，持久脚本 98 行，零参数）→ 期望：`判定=OK：拉不到 CMS 即非零退出、错误可读、无产物` 且 `exit=0`。**为什么补这道**：3.6 的性质此前只有「手拼 env 跑一次构建」的一次性跑法（PROB-027 同类的口径错误——性质成立但没有可复现入口）。**四条判定**：① 死端口先探监听，若竟可连接则判「前提不成立」而非假绿 ② 真实 `pnpm astro:build` 退出码**非零**（只要求非零，理由见 3.6 的 libuv 一条）③ 输出同时含 `ECONNREFUSED` 与该端口号（反证进程 env 确实覆盖了 `.env`）且含「不回退」（证错误可读、口径写明不兜底）④ `apps/astro/dist` 内 HTML 计数为 0（证不会发布空白区块）。**实测**：修 PROB-028 之前即红（`228` `可读错误命中=false`、`neg_gate_exit=1`）⇒ 咬合力自证；修后 `230`（`neg_gate_exit=0`）/ `232`（`neg_gate_r1_exit=0`）/ `233`（`neg_gate_r2_exit=0`）三次 `判定=OK`。**副作用（必须知道再跑）**：Astro 在构建开始就清空 `outDir`，而这条门禁跑的是失败构建 ⇒ 跑完 `dist` 为空，随后任何读 `dist` 的门禁（render-diff / hero-diff / client-bundle）都会硬失败；顺序固定为「跑本条 → `pnpm astro:build` 重建 → 再跑那三道」。

## 4. 交付与归档

- [x] 4.1 临时证据落 `.aiws/tmp/astro-page-copy-cms/`；持久证据 `aiws change evidence astro-page-copy-cms`。**实测**：`aiws change evidence astro-page-copy-cms` → `403` `change_evidence_exit=0`，落 8 份持久工件（`change-status` / `change-validate-strict` / `aiws-validate-stamp` / `change-sync-stamp` / `collaboration-summary` 五个 JSON + `delivery-summary-20260920-224221Z.md`，并把 `review/quality-review.md`、`review/spec-review.md` 归集进证据面），同时把新增工件**逐字同步**进 `proposal.md` 与 plan 的 `Evidence_Path`（两处已比对一致，不产生第二份口径）；`delivery-summary` 记 `Strict validation ok: true`、`Errors: 0`、`Warnings: 0`。**持久面不依赖 gitignored 的 tmp**：台账 318 行的每个 artifact 都已按字节镜像进 `evidence/logs/` 且 sha256 与原件逐条一致（`393`，PROB-033 的口径）。已知限制如实写在 `evidence/verify-before-complete.md` §0（appender 无法登记自己的日志 ⇒ 本轮 `392`–`403` 待下一轮补登后才会进 `evidence/logs/`，提交前必须先补登，否则这些日志随 tmp 一起丢失）
- [x] 4.2 台账推进：REQ-0003 `Impl_Status` TODO → DONE，`REQUIREMENTS.md` 验收勾选并移入「已完成」，CHANGELOG 追加状态行。**实测（脚本 `.aiws/tmp/astro-page-copy-cms/408-advance-req-0003-done.mjs`）**：合同行台账仍 3 行、**改动行=1**、其余两行逐字节不变、键顺序不变，`Impl_Status TODO→DONE`（`Spec_Status` 保持 `READY`，`Evidence` 仍指 `evidence/verify-before-complete.md`）；`REQUIREMENTS.md` 由 120 行 → 123 行，REQ-0003 **整段**从 Backlog 移入「已完成」并加 `✓` 前缀（移完 Backlog 只剩工程治理项说明，不留第二份正文），8 格全勾、全文件 `- [ ]` 残留 **0**，其中第 1 格（label 只有 grep 计数 ⇒ **PROB-045**）与第 6 格（SEO 只到源码级 ⇒ **PROB-039**）按成色勾并在勾旁点名；CHANGELOG **纯追加=true**、表体行 12→13、每条未转义竖线=7、表内空行=-1。**本轮自己造成的红（如实登记，不粉饰）**：`408` `req0003_advance_exit=1`——幂等标记写成「（4.2）」而实际追加行用「（4.2 台账推进）」⇒ 标记永不相等，追加成功后的读回自检必然抛 `markerHits=0`（这个 bug 同时意味着该脚本的重放保护形同虚设）；修 `MARKER` 与单元格逐字一致后复跑 `409` 两支路均打印 `no-op`、`exit=0` ⇒ 幂等已证。**推进真值后的收口链复跑**：`410` `sync_exit=0`（`Changed files: REQUIREMENTS.md`）、`411` `validate_stamp_exit=0`、`412` `change_validate_strict_exit=0`、`413` `change_validate_evidence_scope_exit=0`、`414` doc-claims `判定=OK 问题=0`（打印 `表体行=13`、`PROB-029+ 17 行（DONE 7 / OPEN 10）`），`415` `aiws verify-bc` → `ok: all gates passed (tier=strict)` `verify_bc_exit=0`
- [x] 4.3 `aiws verify-bc` → `aiws commit` → 推送前 L3 深度安全审查 → `aiws change finish astro-page-copy-cms`（注：归档绑在 `--push` 分支，本地路径为 `aiws change archive <id>` + 手工提交，见记忆 `aiws-binding-and-tooling-gaps` 第 13 条）。**本地段已完成，实测**：提交前门禁 `490` → `✓ aiws validate` `validate_exit=0` 与 `ok: all gates passed (tier=strict)` `verify_bc_exit=0`；暂存口径 `git add -A -- . ':!.aiws/memory-bank'` ⇒ `git diff --cached --name-only` 共 589 条路径、其中 memory-bank 命中 **0**，`git status --porcelain` 仍留 3 条 memory-bank 路径未暂存（`.index.yaml` 改动 + `.links.yaml` + `decision/astro-page-copy-cms/` 未跟踪）——即 owner「memory-bank 拆提交」的裁决按字节落实，不是口头；`aiws commit` ⇒ `0872bd0`，`589 files changed, 39811 insertions(+), 2001 deletions(-)`，`git show --name-only HEAD` 里 memory-bank 命中 **0**，**未 `--amend`、未推送**。**剩余两步（各自卡在前置上，不许顺手做）**：推送须 owner 明确确认并先过 L3 深度安全审查；`aiws change finish` 需 CMS 在 `:3000` 可达（404 ≠ 没起）且归档只在 `--push` 分支里。**owner 2026-09-22 点头后两步均已执行，实测**：① 提交前门禁链跑在文档改准之后（`531` `change_sync_exit=0` → `532` `validate_exit=0` → `533` `strict_exit=0` → `534` `evidence_scope_exit=0` → `535` `ok: all gates passed (tier=strict)` `verify_bc_exit=0` → `536` doc-claims `判定=OK`），终局磁盘反查由不带日志的运行报 `归档缺字节（门禁项）=0 判定=OK 问题类合计=0` ⇒ **本轮日志字节无一份随 tmp 消失**；② P15 台账入账与收口链另成 `752d626`（37 条路径、`git diff --cached --name-only` 内 memory-bank 命中 **0**、含 31 份新归档日志），memory-bank 按裁决单独成 `f1ed394`（2 文件），两次都走 `aiws commit`、**未 `--amend`**；③ 推送前置的 L3 深度安全审查对已提交改动跑完 **0 条发现**；④ `aiws change finish astro-page-copy-cms --into main --push --remote gitee` 实测 `finish_exit=2`，但它的**顺序是「合并 + bookkeeping 提交 + push」在前、「归档要 tasks 全勾」在后** ⇒ main 已被快进到 `e7446f2` 并推到 `gitee/main`，随后被 `tasks.md still has unchecked required tasks` 挡住的正是归档，而唯一未勾的行就是本条 4.3——**这是一个自锁**（4.3 的内容包含 finish 本身 ⇒ 带 `--push` 的 finish 不可能在不 `--force` 的情况下自己收尾）。按 `--force` 会跳过门禁、不做，改走记忆里已登记的本地路径：本条勾掉后由独立子命令 `aiws change archive <id>`（不带 finish 的 tasks 门）完成搬运，再手工提交搬运 diff。⑤ 归档即终点 ⇒ **本轮尾部日志的「行」不再补登**（其字节已随 `752d626` 入库，台账只覆盖「可复现的验收判定」这一向，非丢失）；`append-verification-pN.mjs` 与镜像脚本把 `evidence/…` 路径写死，change 目录搬运后不可重跑——这正是 P15 把补账排在推送与归档之前的原因
- [x] 4.4 记忆写入：`aiws memory write decision://astro-page-copy-cms/plan`（规划轮）与交付轮的 schema/接缝决策。**实测**：规划轮 `decision://astro-page-copy-cms/plan`（`created 2026-09-20T10:30:02.956Z`）本已在盘，逐字含方案 B、四集合一比一、唯一读接缝、位置耦合锁长度、`≈1171` 文本单元与去 SITE_ID 兜底同批六条；交付轮 `491` → `✓ Memory written: decision://astro-page-copy-cms/delivery` `memory_write_exit=0`（落 `decision/astro-page-copy-cms/delivery.md`），正文写的是交付期**新**定案而非复述规划：迁移与 project 唯一索引必须显式应用、构建期硬失败的双向证伪、bundle 不得内联 Origin、台账由 `append-verification-pN.mjs` 逐轮追加（行数与只数**不写死**）的三审计器分工、以及「台账覆盖可复现的验收判定 ≠ 执行过的每条命令，任何覆盖率 100% 不成立」与「编号正则必须允许字母后缀」「抛错运行没落 exit 行就不许凭记忆补」四条工具坑；**本轮自己造成的一个口径错（当场修，不粉饰）**：首版正文把日志份数写成死数 `469`，而那个数来自一次**没落日志**的临时统计——既无工件可对面，又必然随下一轮镜像过期（同日终局审计已实测 `编号日志总数=478`）⇒ `499` 重写该条记忆，把份数改成「镜像份数随轮增长、不作口径写死，终局判据是由磁盘反查的那只审计器报 `归档缺字节=0`」，并补进第五条坑「带自己日志的运行永远看不到自己的字节」；重写后读盘确认是**整篇替换而非追加**（`delivery.md` 仍 15 行、`grep -c 469` = 0）；`492` → `Link added: .../delivery -> .../plan` `link_exit=0`，并用 `aiws memory read` **逐字读回**全文（`read_exit=0`）确认写入即自动进索引（`.index.yaml` 追加 2 条 decision、`.links.yaml` 1 条边）。**同一类坑的第二轮（如实登记）**：`499` 去掉了日志份数，但正文另一处仍把台账写成死数「406 行 / appender 21 只」⇒ P15 入账后（`508` `total=460`、`510` 实测 22 只）又过期一次，这次的命中面更广（本条 4.4 也抄了同一个数）。`515` 第三次重写该条记忆：行数与只数改为「随轮增长、刻意不写死」，并把本地提交面写全（`0872bd0` 代码与证据 / `1c530a4` 文档 / `3242886` memory-bank 单独一次，仍未推送未 finish），推送前置按 owner 2026-09-22 的点头更新为「先 L3 再推送、与 finish 一并做」。判据固化：**凡是每轮都会变的量（日志份数、台账行数、appender 只数、缺字节数），文档与记忆里只写判据与出处，不写数值**

## 5. 移交（不计入本 change 完成判定）

- [x] 5.1 线上发布前置（迁移文件已入库但线上不会自动执行；执行顺序必须「先迁移 + 先导入 → 再切 Astro 构建」，否则分站构建 404）落 `evidence/release-prerequisites.md`，标注「线上未执行、未核实」。**本条勾的是「移交清单已落盘」，不是「线上已发布」**：文件顶部即写明本 change 全程只连本地容器库 `127.0.0.1:5434`，线上地址未连接、未执行任何 DDL 或写入；§0 论证了为什么这件事必须人工做（切换后公开站无代码内文案副本，`|| 'juece'` 兜底已删 ⇒ 线上缺 12 条记录或 CMS 不可达时构建硬失败，这是设计性质不是缺陷），§1 给出「`payload.config.ts` 无 `migrations.prodMigrations` ⇒ 生产永不自动 migrate」的实物依据与两条迁移的执行入口，并按 owner「不开维护窗口」的裁决把每步写成可独立择期、失败即回原状的形式
- [x] 5.2 PROB-023（品牌硬编码 7 处）移交 `evidence/follow-ups.md`，作为独立轻量 change 的输入。**PROB-023 仍为 `Status=OPEN`（本 change 未修，只移交）**：`follow-ups.md` §2 起引用该行（7 处位置与「正确落点是站点级品牌配置、与页面集合是两套模型」的不并入理由都在问题台账 `Notes` 里，此处只作索引不复述，遵守 §4 禁双写）；同一文件另把双审查未闭环的 10 条（`PROB-035..041`、`PROB-043`、`PROB-044`、`PROB-045`（4.2 前把「只写在叙述里的缺口」转成可检索 ID）；/`PROB-042` 已在 P11-d 结案为 DONE 故不在列）按优先级排成表，其中 PROB-041 / PROB-039 标为「有窗口期、须在第一次合法改稿前做」
