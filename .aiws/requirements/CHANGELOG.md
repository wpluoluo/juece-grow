# Requirements Changelog

本文件记录“需求变更历史”。`REQUIREMENTS.md` 始终表示“当前有效需求”，变更过程在此追踪。

规则：
- 任何需求变更必须追加一条记录（不要覆盖历史）。
- 记录应可审计：说明变更内容、原因、影响范围、关联 issues/PR。
- 不要写入 secrets。

| 日期 | 变更内容 | 原因/背景 | 影响范围（模块/接口） | 关联 issues/PR | 记录人 |
|---|---|---|---|---|---|
| 2026-08-26 | 新增 REQ-0002：线索跟进提醒自动化（到期 + 首次跟进 SLA，核心） | 现有 nextFollowUpAt 无消费机制，跟进无提醒；new 线索首响无信号 | 新建 reminder-rules / reminder-notices 集合；LeadActivities 增 reminder 枚举；新增 POST /api/v2/reminders/run；看板待办提醒点 | change/lead-followup-reminders | owner |
| 2026-09-19 | 同步 REQUIREMENTS.md 真值：REQ-0001 / REQ-0002 由 Backlog 整体移入「## 已完成」（标题加 ✓ 前缀），13 条验收标准全部勾选为 [x]，并各追加一行复验证据指针；合并原第 27、54 行重复的 `## Backlog` 标题为唯一空区 | 合同 requirements-issues.jsonl 中两条 Spec_Status=READY / Impl_Status=DONE 且 change 已归档于 2026-08-26，根 REQUIREMENTS.md 未同步造成真值漂移 | 文档/合同，无代码与接口变更；复验证据路径已声明，结论待 aiws verify-bc 收口 | change/cleanup-batch-20260919 | owner |
| 2026-09-20 | 自定义端点接口行为收敛：`POST /api/leads/assign` 与 `POST /api/sites/clone` 的「记录不存在」由 `500` 改回 `404`（错误码 `LEAD_NOT_FOUND` / `ASSIGNEE_NOT_FOUND` / `SOURCE_NOT_FOUND`）；`/assign` 成功响应 `data.owner` 由填充后的用户对象收敛为裸 id（不再带出他人 `sessions[]`）；经端点写出的 `lead_activities(type=assigned)` 补齐审计 `actor`；两处 `catch` 改为记服务端日志后再回统一 500 信封 | 清理批 e2e 补齐时登记 PROB-005/006，owner 裁决「不各自立项，合并为一个改动」后随批落地；修复轮按同类模式复查端点带出 PROB-015/016 | 仅两个自定义端点的响应契约，无需求条目变更；仓内无产品侧消费方（admin UI 走 Payload 原生 REST，`grep --exclude-dir=.next` 命中只有注释与 e2e），e2e 断言由敏感键黑名单升级为键集白名单；实测 `Running 60 tests → 58 passed / 2 skipped` 与探针日志见 change 的 `evidence/verification.jsonl` 第 37–41 条 | change/cleanup-batch-20260919 | owner |
| 2026-09-20 | 审计留痕外延修复 + 死权限判断精简：删除项目成员（`Memberships.beforeDelete`）与删除用户（`Users.beforeDelete`）时级联清主的 `payload.update` 透传 `req` ⇒ `Leads.afterChange` 写出的清主动态不再丢 `actor`；`POST /api/leads/assign` 的权限判断删去恒真的 `isProjectMember(...)` 第二项（写权限已蕴含项目成员身份），每次分配少一次 `memberships` 查询，鉴权语义未放宽 | `735bc09` 提交后的独立审查轮把 PROB-006「本地 API 写未透传 `req` ⇒ 丢审计发起人」外延查到底，命中第 3/4 实例；同时按 AGENTS.md §4 清掉恒假/恒真兜底。标记语义（清主被记成 `type=assigned`）需改枚举 ⇒ 另案 PROB-018，本轮不静默扩范围 | 无接口契约与 schema 变更（仅写路径的 `actor` 落库与少一次查询）；补立高风险门禁 `docs/gates/GATE-005-assign-clone-error-and-audit-fix.md`；实测 `npx tsc --noEmit` `exit=0`、`Running 61 tests → 59 passed / 2 skipped`（新增级联审计用例）、库侧 `type='assigned' AND actor_id IS NULL` = `0`，见 `evidence/verification.jsonl` 第 45–50 条 | change/cleanup-batch-20260919 | owner |
| 2026-09-20 | 新增 REQ-0003：公开站页面文案入 CMS（每页一个结构化集合 + 首页 hero/CTA 可排序） | 三站四页营销文案硬编码在 `apps/astro/src/content/*.ts`（1933 行）与 `site.ts`（179 行），改字即发版；`docs/01-plan.md:25` 与 `docs/06-roadmap.md:11` 的「页面区块在后台编辑」至今未落地 | 新增四个页面内容集合与首页可排序区块组；Astro 构建期取数由静态 TS 模块改为读 CMS（CMS 不可达按硬失败，不留代码内兜底副本）；一次性导入脚本搬迁 3 站 × 4 页文案；`docs/03-data-model.md` 的 Page/Block 行随实现口径同步修订 | change/astro-page-copy-cms | owner |
| 2026-09-20 | REQ-0003 验收标准与合同行措辞对齐实现真值：验收 1 的类型真值改指 `apps/astro/src/types/pages/*.ts` 并写明一比一允许的两处受控形变（`titleA`/`titleEm` 折成 `titleLines[]{text,emphasis}`、`FeaturePanel.kind` 由 `blockType` 承载）；验收 6 的 SEO 三件套拆清——标题/描述后台可配（唯一载体是一比一镜像的 `meta.{title,description}`，不设 `seoTitle`/`seoDescription`），canonical 构建期派生不入 CMS；同步修订 `.aiws/requirements/requirements-issues.jsonl` 的 REQ-0003 `Data_Model`，删去「带 sortOrder」与「每条记录含 seo 标题/描述字段」两处失真表述 | 实现轮实测推翻规划轮假设：四个 TS 类型本就带页级 `meta`，12 份快照全有值且四页直送 `Layout`，`Layout.astro:44` 的 `seoTitle \|\| title` 对这四处永不生效 ⇒ `seo*` 是零消费者的第二套载体（AGENTS.md §4 禁双写）；canonical 存库会与三站实际部署域名产生第二真值；`sortOrder` 从未实现（D2 定展示序即 Payload 数组索引序）；类型也在 2.2 从 `content/*.ts` 抽到 `types/pages/*.ts`（顺带更正规划轮「`features.ts:10-14` 内联重复 `IconKey`」的事实错误，该行实为 `FeaturePanel` 五路 `kind` 联合） | 仅需求文档与合同行的措辞/真值对齐，无接口契约与 schema 变更（`seoTitle`/`seoDescription` 从未进过建表迁移，删除发生在 `payload migrate:create` 之前）；四集合覆盖度门禁 `A\B total=0`、CMS `npx tsc --noEmit` 与 `pnpm generate:types` 均 `exit=0`，Astro 侧 `pnpm astro:typecheck`/`astro:build` `exit=0` 且 juece 四页渲染 sha 与基线 4/4 一致，见 `evidence/verification.jsonl` 第 11–25 条 | change/astro-page-copy-cms | owner |
| 2026-09-20 | REQ-0003 合同行随 P4 实现真值改齐（措辞级，范围未变）：`API_Impact` 补登记实际交付的公开读端点 `GET /api/v2/content/pages?site=&page=`（原写「无自研对外端点」已失真——Astro 构建期就是打这条缝），并写明四条原生 REST 仅登录可见（匿名 403 不可枚举）；`Data_Model` 补第三类受控形变＝读侧归一（剥 `id`/`blockName`/`_order` 与记录根 `project`/`status`/时间戳、未填可选字段的 `null` 折回不存在），并注明三处形变的唯一实现处 `apps/cms/src/lib/pageCopyProjection.ts` 与三站 slug 全显式的合同表 `pageCopyContract.ts`；`Preconditions` 补 `apps/astro/.env` 的 `PUBLIC_CMS_ORIGIN`（缺失即抛，不给默认）；`REQUIREMENTS.md` 验收 1 的「两处受控形变」改「三处」 | P4 实测（`.aiws/tmp/astro-page-copy-cms/88`、`96`、`102`）暴露需求描述与实现漂移：往返探针读出 Payload 框架记账键与 `null` 可选字段（P3 未建模），而需求合同仍写「无自研对外端点」，与已交付并 parity 绿过的端点矛盾 ⇒ 按 ws-dev 的 Requirement Sync Gate 先改真值再提交 | 仅需求合同行与 `REQUIREMENTS.md` 的措辞（`API_Impact`/`Data_Model`/`Preconditions` + 验收 1「两处→三处」），无接口契约与 schema 变更；端点、投影与导入脚本的实现证据在同 change 的 `evidence/verification.jsonl` | REQ-0003 / change/astro-page-copy-cms | owner |
| 2026-09-20 | REQ-0003 合同行随 P5 实现真值改齐（措辞级，范围未变）：`Preconditions`/`Inputs` 写明 **SITE_ID 无默认站点**——`dev`/`build`/`preview` 三条脚本 `cross-env` 显式赋值，`astro.config.mjs` 与 `src/site.ts` 各一道「缺 SITE_ID 即抛」，原「缺省即按主站构建」的静默兜底已删；`Business_Logic` 补「静态模块 `apps/astro/src/content/*.ts` 已删 1937 行 ⇒ 代码内不存在可回退的第二来源」；`Tests` 从只列构建/e2e 补成三条零参数逐字门禁（`astro-copy-parity` 端点↔快照、`astro-copy-render-diff` 可见文本、`astro-copy-hero-dom` hero `<h1>` DOM 结构） | P5 实测（`.aiws/tmp/astro-page-copy-cms/125`、`127`、`134`、`135`）把「读 CMS 失败即硬失败」「不改 DOM 结构」从注释承诺变成代码与非零退出门禁，需求合同的 Preconditions/Tests 仍停留在兜底时代 ⇒ 按 ws-dev 的 Requirement Sync Gate 先改真值再提交 | 仅合同行措辞（`Preconditions`/`Inputs`/`Business_Logic`/`Tests`）；代码侧的删兜底与三条脚本显式赋值属 tasks 2.15 的实现范围，不在本行 | REQ-0003 / change/astro-page-copy-cms | owner |
| 2026-09-20 | REQ-0003 合同行 `Tests` 补第四道零参数门禁 `scripts/astro-copy-client-bundle.mjs`（三站产物的 `_astro/*.js` 不得含 `src/site.ts` 的报错文案、Layout 脚本 chunk 的 CMS 地址须为已内联字面量） | P6（tasks 2.17）e2e 抓到 P5（tasks 2.15 删 `SITE_ID` 兜底）引入的客户端回归并登记为 PROB-026：原有五道门禁全在服务端产物层，客户端 bundle 被服务端模块污染时 build/typecheck/parity/render-diff/hero-dom 一律为绿 ⇒ 必须有产物级门禁。实测：修后全量 e2e `170` exit=0（67 passed）、新门禁 `173` exit=0、咬合力自测 `174` exit=0 | 仅合同行 `Tests` 一道门禁；新增脚本 `scripts/astro-copy-client-bundle.mjs` 为产物级只读门禁，不改产品代码与接口 | PROB-026 / change/astro-page-copy-cms | owner |
| 2026-09-21 | REQ-0003 合同行 `Tests` 随 P8 实测改齐（措辞级，范围未变）：把第四条逐字门禁的验收入口由 **三参数抽取器** `scripts/astro-copy-hero-dom.mjs` 换成 P8 新增的 **零参数包装器** `scripts/astro-copy-hero-diff.mjs`（抽取器零参数裸跑只打印用法并 `exit=1`，日志 `200` ⇒ 按合同行复制命令必然复现不出绿灯；登记并当场修毕 `PROB-027`，配咬合力自测 `202`）；并补上同属 tasks 3.3 判据的根入口 `node scripts/build.mjs`，明确其 **libuv 崩溃白名单命中即判不通过**（实测 `196` 真 0 退出） | P8 跑 3.2/3.3 交付门禁时，四道产物门禁复跑链条里 hero DOM 那道裸跑退出码 1 ⇒ 顺藤查出「持久脚本存在」被当成「验收入口存在」的口径错误；按 ws-dev 的 Requirement Sync Gate 先改真值再提交 | 仅合同行 `Tests` 的入口名与判据（抽取器 → 零参数包装器 + 根构建入口）；无接口/schema 变更，新包装器只读 dist 与基线 | PROB-027 / change/astro-page-copy-cms | owner |
| 2026-09-21 | REQ-0003 合同行 `Tests` 随 P9 实测改齐（措辞级，范围未变）：删去「e2e 含 CMS 不可达负向用例」这句**不存在的覆盖**，改为两条实际入口——`node scripts/astro-copy-cms-unreachable.mjs`（P9 新增零参数负向构建门禁；同时结案 PROB-028：连接层失败此前只抛 `fetch failed`，不满足 tasks 3.6 的「可读错误」）与 `pnpm --filter e2e test` 内新增的后台自助改稿用例（tasks 3.5）；并给 `astro-copy-hero-diff.mjs` 标注「迁移期守卫」边界：它按逐字节比 hero DOM，任何合法改稿都会让它红，故不串进常驻构建链 | P9 跑 3.5/3.6 时发现「负向构建」在合同行里被写成一条 e2e 用例、而真实覆盖是构建期 ⇒ 按 ws-dev 的 Requirement Sync Gate 先改真值再提交 | 仅合同行 `Tests`：删一句不存在的覆盖、补两条真实入口；`apps/astro/src/lib/payload.ts` 的错误文案包装与 `scripts/astro-copy-cms-unreachable.mjs` 属同轮实现证据 | PROB-028 / change/astro-page-copy-cms | owner |
| 2026-09-21 | REQ-0003 合同行 `Tests` 随双审查轮（2A.3 / P11）改齐并把验证入口真值落进 `AI_WORKSPACE.md`：`Tests` 补三道本 change 常驻零参数门禁——`node scripts/astro-copy-coverage.mjs`（12 份快照过唯一投影后的路径集合 vs 四集合 schema，「快照有、schema 没有」的差集机器归零 ⇒ 验收 1「一比一镜像」的机器对面）、`node scripts/astro-copy-agents9.mjs`（AGENTS.md §9 自检清单的可机检版：自研文件 ≤1000 行、无兜底/双写、camelCase 三层映射）、`node scripts/astro-copy-evidence-archive.mjs`（台账 artifact 均已镜像进 `evidence/logs/` 且 sha256 与原件一致 ⇒ 证据可跨 clone 复核）；验收 2「首页 hero/CTA 可增删排序」的入口改指常驻 e2e 第 7 组（后台一次写入「追加一行 + 删一行 + 全部倒序」→ 公开站首页 `h1.hero-title > span.line` 与 `.cta-rows .cta-row h3` 逐位等于后台数组序）；`AI_WORKSPACE.md` 验证入口段新增 `page_copy_gates` 清单，并在 `astro_build_cmd` 写明新前置：CMS 必须在 `:3000` 可达，否则构建硬失败（退出码非零、零 HTML 产物、不回退代码内旧文案） | spec/quality 双审查的处置要求「每道门禁都要零参数可复跑且红侧已证」：B-1 指出验收 2 此前只有一次性探针、而唯一常驻 DOM 门禁 `astro-copy-hero-diff.mjs` 判的是「与导入前基线逐字节一致」——合法改稿必然使它红 ⇒ 看不见「改序之后渲染跟不跟着变」；H-2 指出覆盖度/自检两道没有常驻入口；W-6 指出 `AI_WORKSPACE.md` 是入口真值且被 `proposal.md` 列入 In Scope，却在整轮零改动 ⇒ 「怎么跑」没有落处 | 合同行 `Tests`/`Notes` 措辞与 `AI_WORKSPACE.md` 的验证入口段；无接口契约与 schema 变更。三道门禁与 e2e 第 7 组的实测（含各自红侧）见 `evidence/verification.jsonl`；同轮另修：本表第 15 行空行（上个 change 遗留，把一张表切成两段且后段无表头）、P4/P5/P6/P8/P9 五条记录缺的后三列、第 18 行内会串列的字面 `\|\|` | REQ-0003 / PROB-029..PROB-034（本轮自查并当场修毕）/ change/astro-page-copy-cms | owner |
| 2026-09-21 | REQ-0003 交付推进（4.2 台账推进）：合同行 `Impl_Status` TODO → DONE 并刷新 `Updated_At`（`Spec_Status` 保持 READY，`Evidence` 仍指 `evidence/verify-before-complete.md`）；`REQUIREMENTS.md` 把该条从 Backlog **整段移入**「已完成」并加 ✓ 前缀（不留第二份正文，AGENTS.md §4 禁双写），8 格验收全勾；其中第 1 格（一比一＝机器对，双语 label 只有 grep 计数）与第 6 格（SEO 三件套＝源码级机器对，产物级落地值无门禁）按标注的**成色**勾，各指 PROB-045 / PROB-039；同时把此前只写在交付文档叙述里、无 ID 可检索的 label 门禁缺口补登为 **PROB-045**（问题台账 44 → 45 行，复跑 `no-op` 证幂等） | ws-dev 的 Requirement Sync Gate 与完成判定：REQ-0003 的验收真值已按最终态实测成立（九道门禁 P-V1..P-V9 与收口链日志 `370`–`384`、`388`–`391`），spec/quality 双审查 triage 的净结论是「无遗留 HIGH blocker」⇒ 状态位应当推进；而「推进」不等于「宣称全绿」，凡机器对面未覆盖之处必须在勾的同时点名并留 ID（PROB-027 同类的「把叙述当已证」正是本 change 被打过两次的错误） | 仅真值文档与两份台账的状态位/措辞：合同行一个字段、`REQUIREMENTS.md` 的条目位置与勾选态、CHANGELOG 一行、问题台账一行；**无接口契约、无 schema、无产品代码变更**，验收要求本身未放宽（PROB-039 / PROB-045 仍 `OPEN`，成色括注即其可见处） | REQ-0003 / PROB-039 / PROB-045 / change/astro-page-copy-cms | owner |
